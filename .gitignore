# ============================================================================
# IDE / Editor Files
# ============================================================================
# Visual Studio Code
.vscode/
.vs/

# JetBrains IDEs (PyCharm, IntelliJ, etc.)
.idea/
*.iml

# ============================================================================
# Operating System Files
# ============================================================================
# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
Desktop.ini

# Linux
*~
.directory

# ============================================================================
# Python
# ============================================================================
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
env/
ENV/
.venv

# PyInstaller
*.manifest
*.spec

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.log
.pytest_cache/
.hypothesis/

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# ============================================================================
# Terraform
# ============================================================================
# Local .terraform directories
**/.terraform/*
.terraform/
.tfshared/

# Project-local provider cache (filesystem mirror + cli.tfrc + canonical lock)
# Built by scripts/auto_test/cache_setup.sh; never committed, never global.
/.terraform-cache/

# State files
*.tfstate
*.tfstate.*
*.tfstate.backup
.tfshared/

# Lock files — not tracked; regenerated locally from the project-local mirror
# (see scripts/auto_test/cache_setup.sh + unify_provider_versions.py).
**/.terraform.lock.hcl
.terraform.lock.hcl

# Crash log files
crash.log
crash.*.log

# Variable files (may contain sensitive data)
# Exclude all .tfvars files which may contain sensitive data such as
# passwords, private keys, and other secrets
*.tfvars
*.tfvars.json

# Override files (usually used to override resources locally)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform plan files.
#
# NOT `*.json`, and nothing that matches a bare 64-hex name: every fixture dir
# holds its committed plan as `<sha>.json` (see scripts/auto_test/auto_test.py),
# and an ignore rule that swallowed it would silently stop the cache being
# committed — CI would then re-run terraform for the whole tree.
*.tfplan
plan
plan_*.json
plan.json
plan.binary
tfplan
tfplan.json
tfplan.binary

# CLI configuration files
.terraformrc
terraform.rc

# Ignore transient lock info files
.terraform.tfstate.lock.info

**/.terraform/
**/.tfshared/
**/terraform.tfstate*
**/crash.log
# ============================================================================
# Project Specific
# ============================================================================
# docgen generated artifacts
docs/gcp/_history/
docs/gcp/_changes/

# folder_generator local user state (per-user UI selection, never committed)
scripts/folder_generator/cache/

# auto_test --report output: a CI build artifact for the PDE Portal backend,
# published via actions/upload-artifact — never committed to the repo.
policy_results.json
