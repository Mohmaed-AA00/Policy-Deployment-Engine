{
  "last_updated": "2026-06-23T08:26:43Z",
  "provider_version": "7.37.0",
  "arguments": {
    "annotations": {
      "description": "Optional. User defined annotations. See https://google.aip.dev/148#annotations for more details such as format and size limitations\n\n\n**Note**: This field is non-authoritative, and will only manage the annotations present in your configuration.\nPlease refer to the field 'effective_annotations' for all of the annotations present on the resource.",
      "required": false,
      "type": "map(string)",
      "security_impact": false,
      "rationale": "Stores optional user-defined metadata on the policy binding. Annotations are non-authoritative and do not change the binding's access-control behaviour."
    },
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Controls whether Terraform is allowed to destroy the organization policy binding. Using the destroy-blocking value helps prevent accidental destructive loss of the binding and its security configuration during infrastructure changes. This is a reusable prevent-destructive-loss control rather than a runtime IAM permission control."
    },
    "display_name": {
      "description": "Optional. The description of the policy binding. Must be less than or equal to 63 characters.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Provides descriptive text for the policy binding and does not affect policy enforcement."
    },
    "location": {
      "description": "The location of the Policy Binding",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Specifies the IAM policy-binding location. These policy bindings use the global IAM location, so there is no meaningful regional data-residency choice for a platform whitelist to constrain."
    },
    "organization": {
      "description": "The parent organization of the Policy Binding.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Identifies the parent organization containing the policy binding. It is a team-specific resource reference and should not be prescribed by platform policy."
    },
    "policy": {
      "description": "Required. Immutable. The resource name of the policy to be bound. The binding parent and policy must belong to the same Organization (or Project).",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "References the policy that will be bound. Selecting which organization-specific policy to enforce is part of the access-control design and should not be prescribed by a generic platform rule."
    },
    "policy_binding_id": {
      "description": "The Policy Binding ID.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Defines the identifier of the policy binding. Platform policy should not constrain team-specific resource names."
    },
    "policy_kind": {
      "description": "Immutable. The kind of the policy to attach in this binding. This\nfield must be one of the following:  - Left empty (will be automatically set\nto the policy kind) - The input policy kind   Possible values:  POLICY_KIND_UNSPECIFIED PRINCIPAL_ACCESS_BOUNDARY ACCESS",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Identifies the kind of policy attached by the binding. This is a functional policy architecture choice, and the provider restricts the value to supported policy kinds."
    },
    "condition": {
      "type": "block",
      "description": "Represents a textual expression in the Common Expression Language\n(CEL) syntax. CEL is a C-like expression language. The syntax and semantics of\nCEL are documented at https://github.com/google/cel-spec.\nExample (Comparison):\ntitle: \\\"Summary size limit\\\"\ndescription: \\\"Determines if a summary is less than 100 chars\\\"\nexpression: \\\"document.summary.size() < 100\\\"\nExample\n(Equality):\ntitle: \\\"Requestor is owner\\\"\ndescription: \\\"Determines if requestor is the document owner\\\"\nexpression: \\\"document.owner == request.auth.claims.email\\\"  Example\n(Logic):\ntitle: \\\"Public documents\\\"\ndescription: \\\"Determine whether the document should be publicly visible\\\"\nexpression: \\\"document.type != 'private' && document.type != 'internal'\\\"\nExample (Data Manipulation):\ntitle: \\\"Notification string\\\"\ndescription: \\\"Create a notification string with a timestamp.\\\"\nexpression: \\\"'New message received at ' + string(document.create_time)\\\"\nThe exact variables and functions that may be referenced within an expression are\ndetermined by the service that evaluates it. See the service documentation for\nadditional information.",
      "required": false
    },
    "condition.description": {
      "description": "Optional. Description of the expression. This is a longer text which describes the expression, e.g. when hovered over it in a UI.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Provides explanatory text for the binding condition and does not affect enforcement."
    },
    "condition.expression": {
      "description": "Textual representation of an expression in Common Expression Language syntax.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Contains a team-defined CEL expression controlling when the policy binding applies. The correct expression depends on the organisation's access model, so generic constraints would overreach into free-form policy logic."
    },
    "condition.location": {
      "description": "Optional. String indicating the location of the expression for error reporting, e.g. a file name and a position in the file.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Provides source-location information for expression error reporting. It is not the deployment location and has no security effect."
    },
    "condition.title": {
      "description": "Optional. Title for the expression, i.e. a short string describing its purpose. This can be used e.g. in UIs which allow to enter the expression.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Provides a short descriptive title for the condition and does not affect enforcement."
    },
    "target": {
      "type": "block",
      "description": "Target is the full resource name of the resource to which the policy will be bound. Immutable once set.",
      "required": true
    },
    "target.principal_set": {
      "description": "Required. Immutable. Full Resource Name of the principal set used for principal access boundary policy bindings.\nExamples for each one of the following supported principal set types:\n* Organization '//cloudresourcemanager.googleapis.com/organizations/ORGANIZATION_ID'\n* Workforce Identity: '//iam.googleapis.com/locations/global/workforcePools/WORKFORCE_POOL_ID'\n* Workspace Identity: '//iam.googleapis.com/locations/global/workspace/WORKSPACE_ID'\nIt must be parent by the policy binding's parent (the organization).",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "References the principal set to which the policy is bound. Choosing an organization, workforce pool, or workspace target is organization-specific IAM design and cannot be generically constrained without prescribing identities or resource references."
    }
  }
}
