{
  "last_updated": "2026-06-23T08:26:44Z",
  "provider_version": "7.37.0",
  "arguments": {
    "configs": {
      "description": "Connector config as keys/values. The keys of the map are connector property names, for example: 'connector.class', 'tasks.max', 'key.converter'.",
      "required": false,
      "type": "map(string)",
      "security_impact": true,
      "rationale": "Misconfigured or insecure values (e.g., insecure converters or secrets) can lead to data leakage, broken encryption, or operational risks."
    },
    "connect_cluster": {
      "description": "The connect cluster name.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Refers to the parent cluster, not an access control mechanism itself."
    },
    "connector_id": {
      "description": "The ID to use for the connector, which will become the final component of the connector's name. This value is structured like: 'my-connector-id'.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Used for naming only, no impact on security posture."
    },
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "location": {
      "description": "ID of the location of the Kafka Connect resource. See https://cloud.google.com/managed-kafka/docs/locations for a list of supported locations.",
      "required": true,
      "type": "string",
      "security_impact": true,
      "rationale": "Determines the region/zone where the resource is created. Policy should restrict it to an approved-region whitelist for data residency; the allowed set is parameterised (a hardcoded example whitelist is acceptable)."
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Resource scoping identifier; does not influence security directly."
    },
    "task_restart_policy": {
      "type": "block",
      "description": "A policy that specifies how to restart the failed connectors/tasks in a Cluster resource. If not set, the failed connectors/tasks won't be restarted.",
      "required": false
    },
    "task_restart_policy.maximum_backoff": {
      "description": "The maximum amount of time to wait before retrying a failed task. This sets an upper bound for the backoff delay.\nA duration in seconds with up to nine fractional digits, terminated by 's'. Example: \"3.5s\".",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Availability-related; no access or data protection impact."
    },
    "task_restart_policy.minimum_backoff": {
      "description": "The minimum amount of time to wait before retrying a failed task. This sets a lower bound for the backoff delay.\nA duration in seconds with up to nine fractional digits, terminated by 's'. Example: \"3.5s\".",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Availability-related; does not directly influence security posture."
    }
  }
}
