{
  "last_updated": "2026-06-23T08:26:44Z",
  "provider_version": "7.37.0",
  "arguments": {
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Critical safeguard against accidental deletion. If this policy is deleted, traffic inspection and intrusion detection capabilities enabled by packet mirroring disappear, opening up monitoring blind spots."
    },
    "description": {
      "description": "A human-readable description of the rule.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Identifier field documenting packet mirroring rules. No security impact as changing it has no effect on what gets mirrored, where it goes, or whether the policy is active."
    },
    "enable": {
      "description": "Indicates whether or not this packet mirroring takes effect. If set to FALSE, this packet mirroring\npolicy will not be enforced on the network. The default is TRUE. Possible values: [\"TRUE\", \"FALSE\"]",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Master toggle for whether traffic is being copied. This argument has critical security impacts as it determines whether the mirroring, and whatever other monitoring systems depending upon it, is live or dormant."
    },
    "name": {
      "description": "The name of the packet mirroring rule",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Identifier. No security impact as renaming it changes nothing about what's mirrored or where it goes."
    },
    "priority": {
      "description": "Since only one rule can be active at a time, priority is\nused to break ties in the case of two rules that apply to\nthe same instances.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "This argument resolves conflicts, determining which rule's configuration takes effect when rules overlap. This has no security impacts as it configures what traffic is captured."
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "An identification pointer to the external project GCP entity. No security impact as it is a reference."
    },
    "region": {
      "description": "The Region in which the created address should reside.\nIf it is not provided, the provider region is used.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Determines the region/zone where the resource is created. Policy should restrict it to an approved-region whitelist for data residency; the allowed set is parameterised (a hardcoded example whitelist is acceptable)."
    },
    "collector_ilb": {
      "type": "block",
      "description": "The Forwarding Rule resource (of type load_balancing_scheme=INTERNAL)\nthat will be used as collector for mirrored traffic. The\nspecified forwarding rule must have is_mirroring_collector\nset to true.",
      "required": true
    },
    "collector_ilb.url": {
      "description": "The URL of the forwarding rule.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "This argument is the pointer for the collector_ilb block to the load-balancer forwarding rule. It is purely a reference and has no security impacts."
    },
    "filter": {
      "type": "block",
      "description": "A filter for mirrored traffic.  If unset, all traffic is mirrored.",
      "required": false
    },
    "filter.cidr_ranges": {
      "description": "IP CIDR ranges that apply as a filter on the source (ingress) or\ndestination (egress) IP in the IP header. Only IPv4 is supported.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "IPv4 CIDR ranges used to filter mirrored traffic by source or destination IP. The filter argument filters and captures traffic while this argument itself configures its scope."
    },
    "filter.direction": {
      "description": "Direction of traffic to mirror. Default value: \"BOTH\" Possible values: [\"INGRESS\", \"EGRESS\", \"BOTH\"]",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Determines whether inbound traffic, outbound traffic, or both get copied. This argument has no security impacts in that it controls what traffic is captured."
    },
    "filter.ip_protocols": {
      "description": "Possible IP protocols including tcp, udp, icmp and esp",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Restricts mirroring to specific protocols. This argument has no security impacts in that it determines what kind of protocol types are captured."
    },
    "mirrored_resources": {
      "type": "block",
      "description": "A means of specifying which resources to mirror.",
      "required": true
    },
    "mirrored_resources.tags": {
      "description": "All instances with these tags will be mirrored.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Instances carrying specified network tags will be mirrored. This argument has no direct security impacts as it directly determines which instances' traffic gets copied."
    },
    "mirrored_resources.instances": {
      "type": "block",
      "description": "All the listed instances will be mirrored.  Specify at most 50.",
      "required": false
    },
    "mirrored_resources.instances.url": {
      "description": "The URL of the instances where this rule should be active.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "The URL of a specific instance to mirror. Does not have security impact as it is a pointer or reference to an instance."
    },
    "mirrored_resources.subnetworks": {
      "type": "block",
      "description": "All instances in one of these subnetworks will be mirrored.",
      "required": false
    },
    "mirrored_resources.subnetworks.url": {
      "description": "The URL of the subnetwork where this rule should be active.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "The URL of a subnetwork to mirror. Does not have security impacts as it is a pointer or reference to a subnetwork."
    },
    "network": {
      "type": "block",
      "description": "Specifies the mirrored VPC network. Only packets in this network\nwill be mirrored. All mirrored VMs should have a NIC in the given\nnetwork. All mirrored subnetworks should belong to the given network.",
      "required": true
    },
    "network.url": {
      "description": "The full self_link URL of the network where this rule is active.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "The self-link URL of the mirrored network. Does not have security impacts as it is a pointer or reference to the network."
    }
  }
}
