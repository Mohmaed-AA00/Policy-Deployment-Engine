{
  "last_updated": "2026-06-23T08:26:44Z",
  "provider_version": "7.37.0",
  "arguments": {
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "This setting controls whether Terraform can destroy the HA VPN gateway. Setting deletion_policy to PREVENT provides a platform safeguard against accidental or unintended destruction of infrastructure. Preventing destructive resource loss protects the availability and integrity of security-critical network infrastructure, so this argument has a direct security impact."
    },
    "description": {
      "description": "An optional description of this resource.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "This field is only used to describe the VPN gateway. Changing the description does not affect how the gateway works or how secure it is."
    },
    "gateway_ip_version": {
      "description": "The IP family of the gateway IPs for the HA-VPN gateway interfaces. If not specified, IPV4 will be used. Default value: \"IPV4\" Possible values: [\"IPV4\", \"IPV6\"]",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "This field chooses whether the gateway uses IPv4 or IPv6 addresses. Both options are valid depending on the network, so there is no single secure choice for every environment."
    },
    "labels": {
      "description": "Labels for this resource. These can only be added or modified by the setLabels method.\nEach label key/value pair must comply with RFC1035. Label values may be empty.\n\n\n**Note**: This field is non-authoritative, and will only manage the labels present in your configuration.\nPlease refer to the field 'effective_labels' for all of the labels present on the resource.",
      "required": false,
      "type": "map(string)",
      "security_impact": false,
      "rationale": "This field is used to organise and identify the VPN gateway. Labels do not change how network traffic is protected by the gateway."
    },
    "name": {
      "description": "Name of the resource. Provided by the client when the resource is\ncreated. The name must be 1-63 characters long, and comply with\nRFC1035.  Specifically, the name must be 1-63 characters long and\nmatch the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means\nthe first character must be a lowercase letter, and all following\ncharacters must be a dash, lowercase letter, or digit, except the last\ncharacter, which cannot be a dash.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "This field is used to identify the VPN gateway. Changing its name does not change the security settings or protection provided by the gateway."
    },
    "network": {
      "description": "The network this VPN gateway is accepting traffic for.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "This field identifies which network the VPN gateway belongs to. The correct network is different for every organisation, so there is no single security value that applies to everyone."
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "This field identifies which Google Cloud project the VPN gateway belongs to. Project IDs are different for every organisation, so there is no universal security value for this field."
    },
    "region": {
      "description": "The region this gateway should sit in.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Determines the region/zone where the resource is created. Policy should restrict it to an approved-region whitelist for data residency; the allowed set is parameterised (a hardcoded example whitelist is acceptable)."
    },
    "stack_type": {
      "description": "The stack type for this VPN gateway to identify the IP protocols that are enabled.\nIf not specified, IPV4_ONLY will be used. Default value: \"IPV4_ONLY\" Possible values: [\"IPV4_ONLY\", \"IPV4_IPV6\", \"IPV6_ONLY\"]",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "This field decides which IP protocols the gateway supports. IPv4, IPv6 and dual-stack configurations can all be valid depending on the network, so there is no single secure option for every environment."
    },
    "params": {
      "type": "block",
      "description": "Additional params passed with the request, but not persisted as part of resource payload",
      "required": false
    },
    "params.resource_manager_tags": {
      "description": "Resource manager tags to be bound to the HaVpnGateway. Tag keys and values have the\nsame definition as resource manager tags. Keys must be in the format tagKeys/{tag_key_id},\nand values are in the format tagValues/456.",
      "required": false,
      "type": "map(string)",
      "security_impact": false,
      "rationale": "This field adds organisational tags to the VPN gateway. The tags used depend on each organisation's own structure and policies, so there is no single security value that applies to everyone."
    },
    "vpn_interfaces": {
      "type": "block",
      "description": "A list of interfaces on this VPN gateway.",
      "required": false
    },
    "vpn_interfaces.id": {
      "description": "The numeric ID of this VPN gateway interface.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "This field is used to identify a VPN gateway interface. The interface number itself does not determine how securely the gateway operates."
    },
    "vpn_interfaces.interconnect_attachment": {
      "description": "URL of the interconnect attachment resource. When the value\nof this field is present, the VPN Gateway will be used for\nIPsec-encrypted Cloud Interconnect; all Egress or Ingress\ntraffic for this VPN Gateway interface will go through the\nspecified interconnect attachment resource.\n\nNot currently available publicly.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "This field identifies the Interconnect attachment used by the VPN gateway. The correct attachment depends on the organisation's network design, so there is no single security value that works for every environment."
    }
  }
}
