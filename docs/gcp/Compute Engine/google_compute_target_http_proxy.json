{
  "last_updated": "2026-06-23T08:26:44Z",
  "provider_version": "7.37.0",
  "arguments": {
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Deletion-blocking controls help prevent accidental destructive loss of the resource. This argument belongs to the prevent-destructive-loss security archetype because setting deletion_policy to PREVENT blocks Terraform from destroying the Target HTTP Proxy."
    },
    "description": {
      "description": "An optional description of this resource.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Provides optional descriptive metadata only and does not change access control, traffic routing, encryption, network exposure, or the security of the resource."
    },
    "http_keep_alive_timeout_sec": {
      "description": "Specifies how long to keep a connection open, after completing a response,\nwhile there is no matching traffic (in seconds). If an HTTP keepalive is\nnot specified, a default value will be used. For Global\nexternal HTTP(S) load balancer, the default value is 610 seconds, the\nminimum allowed value is 5 seconds and the maximum allowed value is 1200\nseconds. For cross-region internal HTTP(S) load balancer, the default\nvalue is 600 seconds, the minimum allowed value is 5 seconds, and the\nmaximum allowed value is 600 seconds. For Global external HTTP(S) load\nbalancer (classic), this option is not available publicly.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "Controls how long an idle HTTP connection remains open. The appropriate timeout depends on the workload and load-balancer design, so it is primarily a performance and availability setting and has no generic secure value that can be enforced."
    },
    "name": {
      "description": "Name of the resource. Provided by the client when the resource is\ncreated. The name must be 1-63 characters long, and comply with\nRFC1035. Specifically, the name must be 1-63 characters long and match\nthe regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the\nfirst character must be a lowercase letter, and all following\ncharacters must be a dash, lowercase letter, or digit, except the last\ncharacter, which cannot be a dash.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Only identifies the target HTTP proxy and does not change access control, traffic handling, network exposure, or the security of the resource or its data."
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Identifies the Google Cloud project that owns the resource. The approved project is specific to each deployment and is normally controlled through provider configuration, IAM, and organisation policies, so a generic resource policy should not hardcode a project ID."
    },
    "proxy_bind": {
      "description": "This field only applies when the forwarding rule that references\nthis target proxy has a loadBalancingScheme set to INTERNAL_SELF_MANAGED.",
      "required": false,
      "type": "bool",
      "security_impact": true,
      "rationale": "Controls whether Envoy binds to the forwarding rule IP address and port to intercept inbound traffic for INTERNAL_SELF_MANAGED configurations. Enabling it changes network exposure and request handling, so a policy should require false unless gateway or middle-proxy behaviour has been explicitly approved."
    },
    "url_map": {
      "description": "A reference to the UrlMap resource that defines the mapping from URL\nto the BackendService.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "References the URL map that defines routing to backend services. The correct URL map is application-specific, so a generic policy cannot safely restrict it to one identifier. Security controls should instead be applied to the configuration of the referenced URL map."
    }
  }
}
