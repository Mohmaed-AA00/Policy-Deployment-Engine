{
  "last_updated": "2026-06-23T08:26:44Z",
  "provider_version": "7.37.0",
  "arguments": {
    "access_mode": {
      "description": "The access mode of the disk.\nFor example:\n  * READ_WRITE_SINGLE: The default AccessMode, means the disk can be attached to single instance in RW mode.\n  * READ_WRITE_MANY: The AccessMode means the disk can be attached to multiple instances in RW mode.\n  * READ_ONLY_SINGLE: The AccessMode means the disk can be attached to multiple instances in RO mode.\nThe AccessMode is only valid for Hyperdisk disk types.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Multi-writer access allows the same disk to be written by several VMs, which increases the risk of unintended sharing and data corruption. A policy can keep disks in single-writer mode unless multi-writer access is specifically allowed."
    },
    "create_snapshot_before_destroy": {
      "description": "If set to true, a snapshot of the disk will be created before it is destroyed.\nIf your disk is encrypted with customer managed encryption keys these will be reused for the snapshot creation.\nThe name of the snapshot by default will be '{{disk-name}}-YYYYMMDD-HHmm'",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "This is a Terraform destroy-time backup choice. Requiring a snapshot for every disk could retain data that a team intended to delete, so it is not a safe organisation-wide rule; deletion_policy is the direct control for accidental destruction."
    },
    "create_snapshot_before_destroy_prefix": {
      "description": "This will set a custom name prefix for the snapshot that's created when the disk is deleted.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "This only changes the generated snapshot name. It does not affect who can access the snapshot or how its data is encrypted."
    },
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "DELETE allows the data disk to be destroyed, while ABANDON leaves it outside Terraform management. A platform policy can require PREVENT to reduce accidental or unauthorised data loss."
    },
    "description": {
      "description": "An optional description of this resource. Provide this property when\nyou create the resource.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "This is free-text documentation for the disk and does not change access, encryption, or deletion behaviour."
    },
    "image": {
      "description": "The image from which to initialize this disk. This can be\none of: the image's 'self_link', 'projects/{project}/global/images/{image}',\n'projects/{project}/global/images/family/{family}', 'global/images/{image}',\n'global/images/family/{family}', 'family/{family}', '{project}/{family}',\n'{project}/{image}', '{family}', or '{image}'. If referred by family, the\nimages names must include the family name. If they don't, use the\n[google_compute_image data source](/docs/providers/google/d/compute_image.html).\nFor instance, the image 'centos-6-v20180104' includes its family name 'centos-6'.\nThese images can be referred by family name here.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "This is a reference to the image used as a source. A useful allowlist would depend on team-specific image projects, while the source image's own controls should be enforced on the image resource."
    },
    "labels": {
      "description": "Labels to apply to this disk.  A list of key->value pairs.\n\n\n**Note**: This field is non-authoritative, and will only manage the labels present in your configuration.\nPlease refer to the field 'effective_labels' for all of the labels present on the resource.",
      "required": false,
      "type": "map(string)",
      "security_impact": false,
      "rationale": "Labels are metadata for organisation and filtering. They do not grant access to the disk or alter its encryption."
    },
    "licenses": {
      "description": "Any applicable license URI.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "The license URI records software licensing information. It does not control disk permissions or data protection."
    },
    "name": {
      "description": "Name of the resource. Provided by the client when the resource is\ncreated. The name must be 1-63 characters long, and comply with\nRFC1035. Specifically, the name must be 1-63 characters long and match\nthe regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the\nfirst character must be a lowercase letter, and all following\ncharacters must be a dash, lowercase letter, or digit, except the last\ncharacter, which cannot be a dash.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "The name identifies the disk. Google already validates its format, and changing the name does not change the disk's security behaviour."
    },
    "physical_block_size_bytes": {
      "description": "Physical block size of the persistent disk, in bytes. If not present\nin a request, a default value is used. Currently supported sizes\nare 4096 and 16384, other sizes may be added in the future.\nIf an unsupported value is requested, the error message will list\nthe supported values for the caller's project.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "This selects a supported storage block size for compatibility and performance. It does not affect access control or encryption."
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "This identifies the project that owns the disk. Valid project IDs are team-specific, and access to create or use the disk is already governed by project IAM."
    },
    "provisioned_iops": {
      "description": "Indicates how many IOPS to provision for the disk. This sets the number of I/O operations per second\nthat the disk can handle. Values must be between 10,000 and 120,000.\nFor more details, see the Extreme persistent disk [documentation](https://cloud.google.com/compute/docs/disks/extreme-persistent-disk).",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "This sets disk performance capacity in I/O operations per second. It affects performance and cost, not access or data protection."
    },
    "provisioned_throughput": {
      "description": "Indicates how much throughput to provision for the disk. This sets the number of throughput\nmb per second that the disk can handle. Values must be greater than or equal to 1.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "This is a performance setting for disk throughput. A platform security policy has no generally safe throughput value to enforce."
    },
    "region": {
      "description": "A reference to the region where the disk resides.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Determines the region/zone where the resource is created. Policy should restrict it to an approved-region whitelist for data residency; the allowed set is parameterised (a hardcoded example whitelist is acceptable)."
    },
    "replica_zones": {
      "description": "URLs of the zones where the disk should be replicated to.",
      "required": true,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "The replica zones are selected for availability within the disk's region, and Google validates the allowed placement. Exact zones depend on the workload; the region policy already covers data residency."
    },
    "size": {
      "description": "Size of the persistent disk, specified in GB. You can specify this\nfield when creating a persistent disk using the sourceImage or\nsourceSnapshot parameter, or specify it alone to create an empty\npersistent disk.\n\nIf you specify this field along with sourceImage or sourceSnapshot,\nthe value of sizeGb must not be less than the size of the sourceImage\nor the size of the snapshot.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "Disk size is a capacity and cost decision. It does not change encryption, permissions, or network exposure."
    },
    "snapshot": {
      "description": "The source snapshot used to create this disk. You can provide this as\na partial or full URL to the resource. For example, the following are\nvalid values:\n\n* 'https://www.googleapis.com/compute/v1/projects/project/global/snapshots/snapshot'\n* 'projects/project/global/snapshots/snapshot'\n* 'global/snapshots/snapshot'\n* 'snapshot'",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "This is a reference to the snapshot used to populate the disk. The snapshot's access and encryption should be controlled on the snapshot resource rather than by a generic name restriction here."
    },
    "source_disk": {
      "description": "The source disk used to create this disk. You can provide this as a partial or full URL to the resource.\nFor example, the following are valid values:\n\n* https://www.googleapis.com/compute/v1/projects/{project}/zones/{zone}/disks/{disk}\n* https://www.googleapis.com/compute/v1/projects/{project}/regions/{region}/disks/{disk}\n* projects/{project}/zones/{zone}/disks/{disk}\n* projects/{project}/regions/{region}/disks/{disk}\n* zones/{zone}/disks/{disk}\n* regions/{region}/disks/{disk}",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "This points to the disk being cloned. The appropriate source is workload-specific, and security of that disk belongs to the source resource's own policies."
    },
    "type": {
      "description": "URL of the disk type resource describing which disk type to use to\ncreate the disk. Provide this when creating the disk.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "The disk type mainly changes storage technology, performance, and cost. Supported disk types are encrypted by Google by default, so there is no universal security value to require."
    },
    "async_primary_disk": {
      "type": "block",
      "description": "A nested object resource.",
      "required": false
    },
    "async_primary_disk.disk": {
      "description": "Primary disk for asynchronous disk replication.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "This is a reference to the primary disk in an asynchronous replication pair. The correct primary is specific to each disaster-recovery design, and its security is enforced on that disk resource."
    },
    "disk_encryption_key": {
      "type": "block",
      "description": "Encrypts the disk using a customer-supplied encryption key.\n\nAfter you encrypt a disk with a customer-supplied key, you must\nprovide the same key if you use the disk later (e.g. to create a disk\nsnapshot or an image, or to attach the disk to a virtual machine).\n\nCustomer-supplied encryption keys do not protect access to metadata of\nthe disk.\n\nIf you do not provide an encryption key when creating the disk, then\nthe disk will be encrypted using an automatically generated key and\nyou do not need to provide a key to use the disk later.",
      "required": false
    },
    "disk_encryption_key.kms_key_name": {
      "description": "The name of the encryption key that is stored in Google Cloud KMS.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "This selects the Cloud KMS key used to encrypt the new disk. A generic policy can require a non-empty KMS key so the organisation controls key rotation, revocation, and audit logging."
    },
    "disk_encryption_key.raw_key": {
      "description": "Specifies a 256-bit customer-supplied encryption key, encoded in\nRFC 4648 base64 to either encrypt or decrypt this resource.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "A raw customer-supplied key is stored in Terraform configuration and state, creating a direct secret-exposure risk. A platform policy can reject raw_key and require KMS-backed encryption instead."
    },
    "disk_encryption_key.rsa_encrypted_key": {
      "description": "Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit\ncustomer-supplied encryption key to either encrypt or decrypt\nthis resource. You can provide either the rawKey or the rsaEncryptedKey.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "The key material is already RSA-wrapped rather than stored in plaintext. The organisation-wide encryption requirement can be enforced through kms_key_name, so a separate rule on this value adds no general protection."
    },
    "guest_os_features": {
      "type": "block",
      "description": "A list of features to enable on the guest operating system.\nApplicable only for bootable disks.",
      "required": false
    },
    "guest_os_features.type": {
      "description": "The type of supported feature. Read [Enabling guest operating system features](https://cloud.google.com/compute/docs/images/create-delete-deprecate-private-images#guest-os-features) to see a list of available options. Possible values: [\"MULTI_IP_SUBNET\", \"SECURE_BOOT\", \"SEV_CAPABLE\", \"UEFI_COMPATIBLE\", \"VIRTIO_SCSI_MULTIQUEUE\", \"WINDOWS\", \"GVNIC\", \"SEV_LIVE_MIGRATABLE\", \"SEV_SNP_CAPABLE\", \"SUSPEND_RESUME_COMPATIBLE\", \"TDX_CAPABLE\", \"SEV_LIVE_MIGRATABLE_V2\", \"SNP_SVSM_CAPABLE\"]",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "These values describe guest OS capabilities and compatibility. For example, declaring SECURE_BOOT support does not enable Secure Boot on a VM, and the required features vary by operating system."
    },
    "source_image_encryption_key": {
      "type": "block",
      "description": "The customer-supplied encryption key of the source image. Required if\nthe source image is protected by a customer-supplied encryption key.",
      "required": false
    },
    "source_image_encryption_key.kms_key_name": {
      "description": "The name of the encryption key that is stored in Google Cloud KMS.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "This identifies the Cloud KMS key used with an encrypted source image. A policy can require a non-empty KMS key reference when source-image encryption is used, without fixing one team's key name."
    },
    "source_image_encryption_key.kms_key_service_account": {
      "description": "The service account used for the encryption request for the given KMS key.\nIf absent, the Compute Engine Service Agent service account is used.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "This service account performs the KMS request for the source image and therefore forms part of the key-access path. A policy can require an explicitly supplied identity instead of relying on the default service agent."
    },
    "source_image_encryption_key.raw_key": {
      "description": "Specifies a 256-bit customer-supplied encryption key, encoded in\nRFC 4648 base64 to either encrypt or decrypt this resource.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "This places the source image's raw decryption key in Terraform configuration and state. A generic policy can reject raw key use to avoid exposing customer-supplied key material."
    },
    "source_image_encryption_key.rsa_encrypted_key": {
      "description": "Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit\ncustomer-supplied encryption key to either encrypt or decrypt\nthis resource. You can provide either the rawKey or the rsaEncryptedKey.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "This field carries customer-supplied key material in RSA-wrapped form. A policy can disallow this key method when the organisation requires managed KMS keys instead."
    },
    "source_snapshot_encryption_key": {
      "type": "block",
      "description": "The customer-supplied encryption key of the source snapshot. Required\nif the source snapshot is protected by a customer-supplied encryption\nkey.",
      "required": false
    },
    "source_snapshot_encryption_key.raw_key": {
      "description": "Specifies a 256-bit customer-supplied encryption key, encoded in\nRFC 4648 base64 to either encrypt or decrypt this resource.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "This exposes the source snapshot's raw decryption key through Terraform configuration and state. A platform policy can prohibit this field and avoid handling plaintext customer-supplied keys."
    }
  }
}
