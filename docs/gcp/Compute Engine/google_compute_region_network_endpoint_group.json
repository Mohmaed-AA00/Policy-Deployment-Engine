{
  "last_updated": "2026-06-23T08:26:44Z",
  "provider_version": "7.37.0",
  "arguments": {
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Controls whether Terraform is allowed to destroy this endpoint group. A load balancer routes live traffic through the NEG, so an accidental destroy removes the path to the backing service and takes it offline. Policy should require PREVENT so the resource cannot be deleted by a Terraform apply."
    },
    "description": {
      "description": "An optional description of this resource. Provide this property when\nyou create the resource.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Free-text label describing the resource for human readers. It does not affect what the endpoint group connects to or how traffic reaches it, so there is no secure value a policy could enforce."
    },
    "name": {
      "description": "Name of the resource; provided by the client when the resource is\ncreated. The name must be 1-63 characters long, and comply with\nRFC1035. Specifically, the name must be 1-63 characters long and match\nthe regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the\nfirst character must be a lowercase letter, and all following\ncharacters must be a dash, lowercase letter, or digit, except the last\ncharacter, which cannot be a dash.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Identifier for the endpoint group within the project, constrained only by RFC1035 naming rules. Naming affects organisation and readability rather than the security posture of the resource."
    },
    "network": {
      "description": "This field is only used for PSC and INTERNET NEGs.\n\nThe URL of the network to which all network endpoints in the NEG belong. Uses\n\"default\" project network if unspecified.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Reference to the VPC network the endpoints belong to. It selects an existing network rather than configuring one, and the controls that matter (firewall rules, routing, private access) are set on the network resource itself, so there is no secure value to enforce here."
    },
    "network_endpoint_type": {
      "description": "Type of network endpoints in this network endpoint group. Defaults to SERVERLESS. Default value: \"SERVERLESS\" Possible values: [\"SERVERLESS\", \"PRIVATE_SERVICE_CONNECT\", \"INTERNET_IP_PORT\", \"INTERNET_FQDN_PORT\", \"GCE_VM_IP_PORTMAP\"]",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Determines what kind of backends the load balancer sends traffic to. SERVERLESS, PRIVATE_SERVICE_CONNECT and GCE_VM_IP_PORTMAP keep traffic on Google-managed or private paths, while INTERNET_IP_PORT and INTERNET_FQDN_PORT forward it to arbitrary external backends outside the organisation's control. Policy should block the internet endpoint types so traffic is not routed to unmanaged destinations."
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Sets which project owns the resource. It determines ownership and billing scope, not any security setting on the endpoint group; who may access it is decided by project IAM rather than by this field."
    },
    "psc_target_service": {
      "description": "This field is only used for PSC and INTERNET NEGs.\n\nThe target service url used to set up private service connection to\na Google API or a PSC Producer Service Attachment.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Identifies which Google API or producer service attachment the PSC endpoint group connects to. This is a reference to an external endpoint rather than a configurable control, and whether the connection is accepted is decided by the producer's service attachment, not by this field."
    },
    "region": {
      "description": "A reference to the region where the regional NEGs reside.",
      "required": true,
      "type": "string",
      "security_impact": true,
      "rationale": "Determines the region/zone where the resource is created. Policy should restrict it to an approved-region whitelist for data residency; the allowed set is parameterised (a hardcoded example whitelist is acceptable)."
    },
    "subnetwork": {
      "description": "This field is only used for PSC NEGs.\n\nOptional URL of the subnetwork to which all network endpoints in the NEG belong.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Reference to the subnetwork the PSC endpoints draw their addresses from. It selects an existing subnet rather than defining its configuration; the IP range, purpose and Private Google Access settings are all set on the subnetwork resource."
    },
    "app_engine": {
      "type": "block",
      "description": "This field is only used for SERVERLESS NEGs.\n\nOnly one of cloud_run, app_engine, cloud_function or serverless_deployment may be set.",
      "required": false
    },
    "app_engine.service": {
      "description": "Optional serving service.\nThe service name must be 1-63 characters long, and comply with RFC1035.\nExample value: \"default\", \"my-service\".",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Names the App Engine service that receives traffic from this endpoint group. It points at an existing deployment, and that service's own settings (login requirements, TLS enforcement, handlers) are what govern its security, not this reference."
    },
    "app_engine.url_mask": {
      "description": "A template to parse service and version fields from a request URL.\nURL mask allows for routing to multiple App Engine services without\nhaving to create multiple Network Endpoint Groups and backend services.\n\nFor example, the request URLs \"foo1-dot-appname.appspot.com/v1\" and\n\"foo1-dot-appname.appspot.com/v2\" can be backed by the same Serverless NEG with\nURL mask \"-dot-appname.appspot.com/\". The URL mask will parse\nthem to { service = \"foo1\", version = \"v1\" } and { service = \"foo1\", version = \"v2\" } respectively.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Template that lets one endpoint group serve several App Engine services by parsing the service and version out of the request URL. It is a routing convenience that saves creating extra endpoint groups, and it enforces no security property."
    },
    "app_engine.version": {
      "description": "Optional serving version.\nThe version must be 1-63 characters long, and comply with RFC1035.\nExample value: \"v1\", \"v2\".",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Selects which App Engine version serves the traffic. Version choice affects which code runs, but the security configuration of that code is defined on the App Engine version resource rather than here."
    },
    "cloud_function": {
      "type": "block",
      "description": "This field is only used for SERVERLESS NEGs.\n\nOnly one of cloud_run, app_engine, cloud_function or serverless_deployment may be set.",
      "required": false
    },
    "cloud_function.function": {
      "description": "A user-defined name of the Cloud Function.\nThe function name is case-sensitive and must be 1-63 characters long.\nExample value: \"func1\".",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Names the Cloud Function that receives traffic from this endpoint group. It references an existing function, whose authentication, ingress restrictions and runtime settings are configured on the function resource itself."
    },
    "cloud_function.url_mask": {
      "description": "A template to parse function field from a request URL. URL mask allows\nfor routing to multiple Cloud Functions without having to create\nmultiple Network Endpoint Groups and backend services.\n\nFor example, request URLs \"mydomain.com/function1\" and \"mydomain.com/function2\"\ncan be backed by the same Serverless NEG with URL mask \"/\". The URL mask\nwill parse them to { function = \"function1\" } and { function = \"function2\" } respectively.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Template that lets one endpoint group serve several Cloud Functions by parsing the function name out of the request URL. It is a routing convenience with no security control attached."
    },
    "cloud_run": {
      "type": "block",
      "description": "This field is only used for SERVERLESS NEGs.\n\nOnly one of cloud_run, app_engine, cloud_function or serverless_deployment may be set.",
      "required": false
    },
    "cloud_run.service": {
      "description": "Cloud Run service is the main resource of Cloud Run.\nThe service must be 1-63 characters long, and comply with RFC1035.\nExample value: \"run-service\".",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Names the Cloud Run service that receives traffic from this endpoint group. It references an existing service, whose ingress settings, authentication requirements and TLS configuration are defined on the Cloud Run resource."
    },
    "cloud_run.tag": {
      "description": "Cloud Run tag represents the \"named-revision\" to provide\nadditional fine-grained traffic routing information.\nThe tag must be 1-63 characters long, and comply with RFC1035.\nExample value: \"revision-0010\".",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Selects a named revision of the Cloud Run service so traffic can be split at a finer granularity. It chooses which revision serves requests rather than setting any security property on that revision."
    },
    "cloud_run.url_mask": {
      "description": "A template to parse service and tag fields from a request URL.\nURL mask allows for routing to multiple Run services without having\nto create multiple network endpoint groups and backend services.\n\nFor example, request URLs \"foo1.domain.com/bar1\" and \"foo1.domain.com/bar2\"\nan be backed by the same Serverless Network Endpoint Group (NEG) with\nURL mask \".domain.com/\". The URL mask will parse them to { service=\"bar1\", tag=\"foo1\" }\nand { service=\"bar2\", tag=\"foo2\" } respectively.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Template that lets one endpoint group serve several Cloud Run services by parsing the service and tag out of the request URL. It is a routing convenience and enforces no security property."
    },
    "psc_data": {
      "type": "block",
      "description": "This field is only used for PSC NEGs.",
      "required": false
    },
    "psc_data.producer_port": {
      "description": "The PSC producer port to use when consumer PSC NEG connects to a producer. If\nthis flag isn't specified for a PSC NEG with endpoint type\nprivate-service-connect, then PSC NEG will be connected to a first port in the\navailable PSC producer port range.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Chooses which port from the producer's published range the consumer connects on. It is a connectivity detail within an already-established private connection, and whether that connection is permitted is decided by the producer's service attachment."
    }
  }
}