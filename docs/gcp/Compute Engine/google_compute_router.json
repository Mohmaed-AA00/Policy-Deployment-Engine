{
  "last_updated": "2026-06-23T08:26:44Z",
  "provider_version": "7.37.0",
  "arguments": {
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "ABANDON removes the router from Terraform management while leaving it live, creating unmanaged drift outside IaC governance; unprotected deletion severs NAT/VPN/Interconnect connectivity. Policy should blacklist ABANDON."
    },
    "description": {
      "description": "An optional description of this resource.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Free-text metadata with no effect on behaviour or exposure."
    },
    "encrypted_interconnect_router": {
      "description": "Indicates if a router is dedicated for use with encrypted VLAN\nattachments (interconnectAttachments).",
      "required": false,
      "type": "bool",
      "security_impact": true,
      "rationale": "Dedicates the router to encrypted VLAN attachments, enforcing encryption in transit for Interconnect traffic. Policy should require true for routers carrying Interconnect workloads."
    },
    "name": {
      "description": "Name of the resource. The name must be 1-63 characters long, and\ncomply with RFC1035. Specifically, the name must be 1-63 characters\nlong and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?'\nwhich means the first character must be a lowercase letter, and all\nfollowing characters must be a dash, lowercase letter, or digit,\nexcept the last character, which cannot be a dash.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Resource identifier; format is API-enforced (RFC1035) and naming carries no security control."
    },
    "network": {
      "description": "A reference to the network to which this router belongs.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Reference selecting which VPC the router serves; a placement/targeting choice whose security is governed by the network's own controls."
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Deployment-target reference; access to the project is governed by IAM, not by this argument."
    },
    "region": {
      "description": "Region where the router resides.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Determines the region/zone where the resource is created. Policy should restrict it to an approved-region whitelist for data residency; the allowed set is parameterised (a hardcoded example whitelist is acceptable)."
    },
    "bgp": {
      "type": "block",
      "description": "BGP information specific to this router.",
      "required": false
    },
    "bgp.advertise_mode": {
      "description": "User-specified flag to indicate which mode to use for advertisement. Default value: \"DEFAULT\" Possible values: [\"DEFAULT\", \"CUSTOM\"]",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "DEFAULT auto-advertises all subnets to peers. Policy should require CUSTOM so route advertisement is explicit and least-privilege."
    },
    "bgp.advertised_groups": {
      "description": "User-specified list of prefix groups to advertise in custom mode.\nThis field can only be populated if advertiseMode is CUSTOM and\nis advertised to all peers of the router. These groups will be\nadvertised in addition to any specified prefixes. Leave this field\nblank to advertise no custom groups.\n\nThis enum field has the one valid value: ALL_SUBNETS",
      "required": false,
      "type": "list(string)",
      "security_impact": true,
      "rationale": "Advertising the ALL_SUBNETS group re-creates blanket advertisement even under CUSTOM mode. Policy should blacklist ALL_SUBNETS from the list."
    },
    "bgp.asn": {
      "description": "Local BGP Autonomous System Number (ASN). Must be an RFC6996\nprivate ASN, either 16-bit or 32-bit. The value will be fixed for\nthis router resource. All VPN tunnels that link to this router\nwill have the same local ASN.",
      "required": true,
      "type": "number",
      "security_impact": false,
      "rationale": "Local ASN; the API already restricts it to RFC6996 private ASN space, so no insecure value is expressible."
    },
    "bgp.identifier_range": {
      "description": "Explicitly specifies a range of valid BGP Identifiers for this Router.\nIt is provided as a link-local IPv4 range (from 169.254.0.0/16), of\nsize at least /30, even if the BGP sessions are over IPv6. It must\nnot overlap with any IPv4 BGP session ranges. Other vendors commonly\ncall this router ID.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Link-local (169.254.0.0/16) BGP identifier allocation; internal session plumbing with no exposure."
    },
    "bgp.keepalive_interval": {
      "description": "The interval in seconds between BGP keepalive messages that are sent\nto the peer. Hold time is three times the interval at which keepalive\nmessages are sent, and the hold time is the maximum number of seconds\nallowed to elapse between successive keepalive messages that BGP\nreceives from a peer.\n\nBGP will use the smaller of either the local hold time value or the\npeer's hold time value as the hold time for the BGP connection\nbetween the two peers. If set, this value must be between 20 and 60.\nThe default is 20.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "BGP session timing tuning; affects failover speed, not security posture."
    },
    "bgp.advertised_ip_ranges": {
      "type": "block",
      "description": "User-specified list of individual IP ranges to advertise in\ncustom mode. This field can only be populated if advertiseMode\nis CUSTOM and is advertised to all peers of the router. These IP\nranges will be advertised in addition to any specified groups.\nLeave this field blank to advertise no custom IP ranges.",
      "required": false
    },
    "bgp.advertised_ip_ranges.description": {
      "description": "User-specified description for the IP range.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Free-text metadata with no effect on behaviour or exposure."
    },
    "bgp.advertised_ip_ranges.range": {
      "description": "The IP range to advertise. The value must be a\nCIDR-formatted string.",
      "required": true,
      "type": "string",
      "security_impact": true,
      "rationale": "CIDR ranges advertised to BGP peers. Over-broad ranges (e.g. 0.0.0.0/0 or whole-RFC1918 blocks) leak internal reachability beyond need. Policy should blacklist over-broad prefixes."
    },
    "md5_authentication_keys": {
      "type": "block",
      "description": "Keys used for MD5 authentication.",
      "required": false
    },
    "md5_authentication_keys.key": {
      "description": "Value of the key used for MD5 authentication.",
      "required": true,
      "type": "string",
      "security_impact": true,
      "rationale": "BGP MD5 authentication secret protecting peering sessions against spoofing and route hijack. A blank or trivially guessable shared secret leaves the session effectively unauthenticated, allowing an on-path attacker to inject or modify route advertisements. Keys must be of sufficient length and complexity and should be sourced from a secret manager rather than committed to configuration or state."
    },
    "md5_authentication_keys.name": {
      "description": "Name used to identify the key. Must be unique within a router.\nMust be referenced by exactly one bgpPeer. Must comply with RFC1035.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Identifier label for the key referenced by a BGP peer; the secret itself is the key argument, not this name."
    },
    "params": {
      "type": "block",
      "description": "Additional params passed with the request, but not persisted as part of resource payload",
      "required": false
    },
    "params.resource_manager_tags": {
      "description": "Resource manager tags to be bound to the router. Tag keys and values have the\nsame definition as resource manager tags. Keys must be in the format tagKeys/{tag_key_id},\nand values are in the format tagValues/456.",
      "required": false,
      "type": "map(string)",
      "security_impact": false,
      "rationale": "Resource manager tags bound at create time; organisational metadata whose access effects are governed by IAM conditions elsewhere."
    }
  }
}

