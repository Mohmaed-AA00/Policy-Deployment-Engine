{
  "last_updated": "2026-06-23T08:26:44Z",
  "provider_version": "7.37.0",
  "arguments": {
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "If deletion_policy is left at the default value of DELETE, the autoscaler can be destroyed accidentally via terraform destroy or apply, removing active scaling management and potentially causing a service outage. Policy should require deletion_policy to be set to PREVENT."
    },
    "description": {
      "description": "An optional description of this resource.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "description is a free-text label field with no functional effect on resource behaviour, so it carries no security impact."
    },
    "name": {
      "description": "Name of the resource. The name must be 1-63 characters long and match\nthe regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the\nfirst character must be a lowercase letter, and all following\ncharacters must be a dash, lowercase letter, or digit, except the last\ncharacter, which cannot be a dash.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "name is a required identifier constrained by GCP's own naming regex, it does not control any behaviour, permissions, or scaling logic, so no direct security impact is identified."
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "project is an identifier/reference field specifying deployment context rather than a direct access, exposure, or data control, so no security impact is assigned per project classification guidance."
    },
    "region": {
      "description": "URL of the region where the instance group resides.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Determines the region/zone where the resource is created. Policy should restrict it to an approved-region whitelist for data residency; the allowed set is parameterised (a hardcoded example whitelist is acceptable)."
    },
    "target": {
      "description": "URL of the managed instance group that this autoscaler will scale.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "target is a reference/pointer to another resource (the managed instance group), security is enforced on the referenced resource itself, not the pointer, so no security impact is assigned."
    },
    "autoscaling_policy": {
      "type": "block",
      "description": "The configuration parameters for the autoscaling algorithm. You can\ndefine one or more of the policies for an autoscaler: cpuUtilization,\ncustomMetricUtilizations, and loadBalancingUtilization.\n\nIf none of these are specified, the default will be to autoscale based\non cpuUtilization to 0.6 or 60%.",
      "required": true
    },
    "autoscaling_policy.cooldown_period": {
      "description": "The number of seconds that the autoscaler should wait before it\nstarts collecting information from a new instance. This prevents\nthe autoscaler from collecting information when the instance is\ninitializing, during which the collected usage would not be\nreliable. The default time autoscaler waits is 60 seconds.\n\nVirtual machine initialization times might vary because of\nnumerous factors. We recommend that you test how long an\ninstance may take to initialize. To do this, create an instance\nand time the startup process.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "cooldown_period is an operational tuning parameter governing scaling responsiveness, not a security or access control, so no security impact is assigned."
    },
    "autoscaling_policy.max_replicas": {
      "description": "The maximum number of instances that the autoscaler can scale up\nto. This is required when creating or updating an autoscaler. The\nmaximum number of replicas should not be lower than minimal number\nof replicas.",
      "required": true,
      "type": "number",
      "security_impact": false,
      "rationale": "max_replicas is an operational/budgetary capacity cap that depends on workload requirements rather than a platform security policy, so no security impact is assigned."
    },
    "autoscaling_policy.min_replicas": {
      "description": "The minimum number of replicas that the autoscaler can scale down\nto. This cannot be less than 0. If not provided, autoscaler will\nchoose a default value depending on maximum number of instances\nallowed.",
      "required": true,
      "type": "number",
      "security_impact": true,
      "rationale": "If min_replicas is set to 0, the autoscaler can scale down to zero running servers, causing a denial-of-service outcome (an availability failure under the CIA triad) even without any attacker involved. policy should require min_replicas to be atleast 1."
    },
    "autoscaling_policy.mode": {
      "description": "Defines operating mode for this policy.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "If mode is set to OFF, the autoscaler stops responding to load entirely, leaving the instance group fixed in size and unable to scale out during a traffic spike, an availability failure under the CIA triad. Policy should require mode to be set to ON (or ONLY_UP at minimum) so the autoscaler remains active"
    },
    "autoscaling_policy.stabilization_period": {
      "description": "The number of seconds that the autoscaler waits for load stabilization\nbefore making scale-in decisions.\n\nThis might appear as a delay in scaling in but it is an important mechanism\nfor your application to not have fluctuating size due to short term load\nfluctuations.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "stabilization_period is operational performance management governing scale-in timing, not a security or availability control, so no security impact is assigned."
    },
    "autoscaling_policy.cpu_utilization": {
      "type": "block",
      "description": "Defines the CPU utilization policy that allows the autoscaler to\nscale based on the average CPU utilization of a managed instance\ngroup.",
      "required": false
    },
    "autoscaling_policy.cpu_utilization.predictive_method": {
      "description": "Indicates whether predictive autoscaling based on CPU metric is enabled. Valid values are:\n\n- NONE (default). No predictive method is used. The autoscaler scales the group to meet current demand based on real-time metrics.\n\n- OPTIMIZE_AVAILABILITY. Predictive autoscaling improves availability by monitoring daily and weekly load patterns and scaling out ahead of anticipated demand.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "predictive_method controls whether scaling uses historical load patterns to anticipate demand versus reacting only to real-time metrics. It's a performance/smoothness optimisation rather than a control whose misconfiguaration creates a secuirty or availability gap, so no security impact is assigned."
    },
    "autoscaling_policy.cpu_utilization.target": {
      "description": "The target CPU utilization that the autoscaler should maintain.\nMust be a float value in the range (0, 1]. If not specified, the\ndefault is 0.6.\n\nIf the CPU level is below the target utilization, the autoscaler\nscales down the number of instances until it reaches the minimum\nnumber of instances you specified or until the average CPU of\nyour instances reaches the target utilization.\n\nIf the average CPU is above the target utilization, the autoscaler\nscales up until it reaches the maximum number of instances you\nspecified or until the average utilization reaches the target\nutilization.",
      "required": true,
      "type": "number",
      "security_impact": false,
      "rationale": "cpu_utilization target is a performance and capacity sizing choice rather than a security or availability control, so no security impact is assigned."
    },
    "autoscaling_policy.load_balancing_utilization": {
      "type": "block",
      "description": "Configuration parameters of autoscaling based on a load balancer.",
      "required": false
    },
    "autoscaling_policy.load_balancing_utilization.target": {
      "description": "Fraction of backend capacity utilization (set in HTTP(s) load\nbalancing configuration) that autoscaler should maintain. Must\nbe a positive float value. If not defined, the default is 0.8.",
      "required": true,
      "type": "number",
      "security_impact": false,
      "rationale": "load_balancing_utilization target is a capacity/performance sizing choice rather than a platform security policy target, so no security impact is assigned."
    },
    "autoscaling_policy.metric": {
      "type": "block",
      "description": "Configuration parameters of autoscaling based on a custom metric.",
      "required": false
    },
    "autoscaling_policy.metric.filter": {
      "description": "A filter string to be used as the filter string for\na Stackdriver Monitoring TimeSeries.list API call.\nThis filter is used to select a specific TimeSeries for\nthe purpose of autoscaling and to determine whether the metric\nis exporting per-instance or per-group data.\n\nYou can only use the AND operator for joining selectors.\nYou can only use direct equality comparison operator (=) without\nany functions for each selector.\nYou can specify the metric in both the filter string and in the\nmetric field. However, if specified in both places, the metric must\nbe identical.\n\nThe monitored resource type determines what kind of values are\nexpected for the metric. If it is a gce_instance, the autoscaler\nexpects the metric to include a separate TimeSeries for each\ninstance in a group. In such a case, you cannot filter on resource\nlabels.\n\nIf the resource type is any other value, the autoscaler expects\nthis metric to contain values that apply to the entire autoscaled\ninstance group and resource label filtering can be performed to\npoint autoscaler at the correct TimeSeries to scale upon.\nThis is called a per-group metric for the purpose of autoscaling.\n\nIf not specified, the type defaults to gce_instance.\n\nYou should provide a filter that is selective enough to pick just\none TimeSeries for the autoscaled group or for each of the instances\n(if you are using gce_instance resource type). If multiple\nTimeSeries are returned upon the query execution, the autoscaler\nwill sum their respective values to obtain its scaling value.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "metric.filter is a query/reference string pointing to telemetry data sources, enforcing a generic policy on it would overreach, so no security impact is assigned."
    },
    "autoscaling_policy.metric.name": {
      "description": "The identifier (type) of the Stackdriver Monitoring metric.\nThe metric cannot have negative values.\n\nThe metric must have a value type of INT64 or DOUBLE.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "metric.name is an identifier pointing to a specific telemetry signal, making it a team-specific configuration reference rather than a platform policy target, so no security impact is assigned."
    },
    "autoscaling_policy.metric.single_instance_assignment": {
      "description": "If scaling is based on a per-group metric value that represents the\ntotal amount of work to be done or resource usage, set this value to\nan amount assigned for a single instance of the scaled group.\nThe autoscaler will keep the number of instances proportional to the\nvalue of this metric, the metric itself should not change value due\nto group resizing.\n\nFor example, a good metric to use with the target is\n'pubsub.googleapis.com/subscription/num_undelivered_messages'\nor a custom metric exporting the total number of requests coming to\nyour instances.\n\nA bad example would be a metric exporting an average or median\nlatency, since this value can't include a chunk assignable to a\nsingle instance, it could be better used with utilization_target\ninstead.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "single_instance_assignment is a capacity sizing and performance threshold, an operational choice rather than a security control, so no security impact is assigned."
    },
    "autoscaling_policy.metric.target": {
      "description": "The target value of the metric that autoscaler should\nmaintain. This must be a positive value. A utilization\nmetric scales number of virtual machines handling requests\nto increase or decrease proportionally to the metric.\n\nFor example, a good metric to use as a utilizationTarget is\nwww.googleapis.com/compute/instance/network/received_bytes_count.\nThe autoscaler will work to keep this value constant for each\nof the instances.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "metric.target is a workload performance target and scaling threshold, an operational parameter rather than a security policy, so no security impact is assigned."
    },
    "autoscaling_policy.metric.type": {
      "description": "Defines how target utilization value is expressed for a\nStackdriver Monitoring metric. Possible values: [\"GAUGE\", \"DELTA_PER_SECOND\", \"DELTA_PER_MINUTE\"]",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "type only defines how the metric's numeric value is interpreted (gauge vs delta); it does not itself introduce a misconfiguration risk independent of the metric.target and metric.filer settings already covered."
    },
    "autoscaling_policy.scale_in_control": {
      "type": "block",
      "description": "Defines scale in controls to reduce the risk of response latency\nand outages due to abrupt scale-in events",
      "required": false
    },
    "autoscaling_policy.scale_in_control.time_window_sec": {
      "description": "How long back autoscaling should look when computing recommendations\nto include directives regarding slower scale down, as described above.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "time_window_sec is an autoscaling timing/rate-limit parameter for operational reliability and availability tuning, not a security control, so no security impact is assigned."
    },
    "autoscaling_policy.scale_in_control.max_scaled_in_replicas": {
      "type": "block",
      "description": "A nested object resource.",
      "required": false
    },
    "autoscaling_policy.scale_in_control.max_scaled_in_replicas.fixed": {
      "description": "Specifies a fixed number of VM instances. This must be a positive\ninteger.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "max_scaled_in_replicas.fixed is a scale-in rate limit and replica bound, an operational availability parameter rather than a platform security policy, so no security impact is assigned."
    },
    "autoscaling_policy.scale_in_control.max_scaled_in_replicas.percent": {
      "description": "Specifies a percentage of instances between 0 to 100%, inclusive.\nFor example, specify 80 for 80%.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "max_scaled_in_replicas.percent is a scaling limit and operational control rather than a security policy, so no security impact is assigned."
    },
    "autoscaling_policy.scaling_schedules": {
      "type": "block",
      "description": "Scaling schedules defined for an autoscaler. Multiple schedules can be set on an autoscaler and they can overlap.",
      "required": false
    },
    "autoscaling_policy.scaling_schedules.description": {
      "description": "A description of a scaling schedule.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "description is a free-text label field with no functional effect on resource behaviour, so it carries no security impact."
    },
    "autoscaling_policy.scaling_schedules.disabled": {
      "description": "A boolean value that specifies if a scaling schedule can influence autoscaler recommendations. If set to true, then a scaling schedule has no effect.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "disabled toggles whether a scheduled scaling window is active, an operational workload management choice rather than a security control, so no security impact is assigned."
    },
    "autoscaling_policy.scaling_schedules.duration_sec": {
      "description": "The duration of time intervals (in seconds) for which this scaling schedule will be running. The minimum allowed value is 300.",
      "required": true,
      "type": "number",
      "security_impact": false,
      "rationale": "duration_sec is an operational availability/sizing choice governing schedule timing, a team decision that does not affect platform security or exposure, so no security impact is assigned."
    },
    "autoscaling_policy.scaling_schedules.min_required_replicas": {
      "description": "Minimum number of VM instances that autoscaler will recommend in time intervals starting according to schedule.",
      "required": true,
      "type": "number",
      "security_impact": false,
      "rationale": "min_required_replicas is a capacity sizing decision, an operational availability choice rather than a security control, so no security impact is assigned."
    },
    "autoscaling_policy.scaling_schedules.name": {
      "description": "N/A",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "name is an identifier label for the schedule with no functional effect on scaling behaviour, so it carries no security impact."
    },
    "autoscaling_policy.scaling_schedules.schedule": {
      "description": "The start timestamps of time intervals when this scaling schedule should provide a scaling signal. This field uses the extended cron format (with an optional year field).",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "schedule is a cron expression governing scaling timing, an operational scheduling choice rather than a security control, so no security impact is assigned."
    },
    "autoscaling_policy.scaling_schedules.time_zone": {
      "description": "The time zone to be used when interpreting the schedule. The value of this field must be a time zone name from the tz database: http://en.wikipedia.org/wiki/Tz_database.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "time_zone is an operational scheduling choice for interpreting the schedule, not a security control, so no security impact is assigned."
    }
  }
}
