{
  "last_updated": "2026-06-23T08:26:44Z",
  "provider_version": "7.37.0",
  "arguments": {
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Controls how the resource gets removed. Allowing ABANDON leaves it orphaned outside Terraform, so this is worth enforcing, so true."
    },
    "description": {
      "description": "An optional description of this resource.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Just a free text description. It doesn't affect how the resource works or its security, so false."
    },
    "dns_verification_ip": {
      "description": "The IPv4 address to be used for reverse DNS verification.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "This is just the IP used for reverse DNS verification. It's a functional value, not a security control, so false."
    },
    "ip_cidr_range": {
      "description": "The address range, in CIDR format, represented by this public advertised prefix.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "This is the address range the prefix advertises. It's the core setup of the resource, not a security value with a safe or unsafe setting to enforce, so false."
    },
    "ipv6_access_type": {
      "description": "The internet access type for IPv6 Public Advertised Prefixes. It can be\nset to one of following:\n  * EXTERNAL: Default access type. The prefix will be announced to the\n  internet. All children PDPs will have access type as EXTERNAL.\n  * INTERNAL: The prefix won’t be announced to the internet. Prefix will\n  be used privately within Google Cloud. All children PDPs will have\n  access type as INTERNAL. Possible values: [\"EXTERNAL\", \"INTERNAL\"]",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "This argument dictates the architectural routing mode of the prefix (external vs internal use), which is an intentional design choice for publicly routing enterprise IP space rather than a generic security control."
    },
    "name": {
      "description": "Name of the resource. The name must be 1-63 characters long, and\ncomply with RFC1035. Specifically, the name must be 1-63 characters\nlong and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?'\nwhich means the first character must be a lowercase letter, and all\nfollowing characters must be a dash, lowercase letter, or digit,\nexcept the last character, which cannot be a dash.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "The name is just an identifier for the resource, not something you put a security rule on, so this is false."
    },
    "pdp_scope": {
      "description": "Specifies how child public delegated prefix will be scoped. pdpScope\nmust be one of: GLOBAL, REGIONAL\n* REGIONAL: The public delegated prefix is regional only. The\nprovisioning will take a few minutes.\n* GLOBAL: The public delegated prefix is global only. The provisioning\nwill take ~4 weeks. Possible values: [\"GLOBAL\", \"REGIONAL\"]",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "This just sets whether child prefixes are global or regional. It's a scoping choice, not a security control, so false."
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Project just says where the resource lives, it's not a security control on the resource itself, so false."
    }
  }
}
