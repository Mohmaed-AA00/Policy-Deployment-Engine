{
  "last_updated": "2026-06-23T08:26:44Z",
  "provider_version": "7.37.0",
  "arguments": {
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Deletion-blocking flags follow the prevent-destructive-loss archetype: enforcing PREVENT protects against accidental or malicious destruction of the resource, so this is treated as security-relevant."
    },
    "description": {
      "description": "An optional description of this resource.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "A free-text label with no functional effect on access, exposure, or data protection."
    },
    "name": {
      "description": "Name of the resource. Provided by the client when the resource is\ncreated. The name must be 1-63 characters long, and comply with\nRFC1035. Specifically, the name must be 1-63 characters long and match\nthe regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the\nfirst character must be a lowercase letter, and all following\ncharacters must be a dash, lowercase letter, or digit, except the last\ncharacter, which cannot be a dash.\n\nThese are in the same namespace as the managed SSL certificates.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "An identifier for the resource; it does not itself change security behaviour."
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "The project determines resource ownership/location, similar to other identifier fields; it is not itself a security control."
    },
    "type": {
      "description": "Enum field whose value is always 'MANAGED' - used to signal to the API\nwhich type this is. Default value: \"MANAGED\" Possible values: [\"MANAGED\"]",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Currently only one allowed value (MANAGED); it identifies the certificate mode rather than itself controlling exposure or access."
    },
    "managed": {
      "type": "block",
      "description": "Properties relevant to a managed certificate.  These will be used if the\ncertificate is managed (as indicated by a value of 'MANAGED' in 'type').",
      "required": false
    },
    "managed.domains": {
      "description": "Domains for which a managed SSL certificate will be valid.  Currently,\nthere can be up to 100 domains in this list.",
      "required": true,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Domains are team-specific configuration values that cannot be known or validated at the platform level, making them an identifier/reference rather than a generic security control. Google's certificate issuance process already enforces domain validation before a certificate is issued, so this is not a gap a Terraform-level policy needs to cover."
    }
  }
}
