{
  "last_updated": "2026-06-23T08:26:44Z",
  "provider_version": "7.37.0",
  "arguments": {
    "alternative_location_id": {
      "description": "Only applicable to STANDARD_HA tier which protects the instance\nagainst zonal failures by provisioning it across two zones.\nIf provided, it must be a different zone from the one provided in\n[locationId].",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Specifies the secondary zone used for high availability. It is primarily an availability and resilience setting rather than a platform security control."
    },
    "auth_enabled": {
      "description": "Optional. Indicates whether OSS Redis AUTH is enabled for the\ninstance. If set to \"true\" AUTH is enabled on the instance.\nDefault value is \"false\" meaning AUTH is disabled.",
      "required": false,
      "type": "bool",
      "security_impact": true,
      "rationale": "Controls whether Redis AUTH is enabled. Requiring authentication helps prevent unauthenticated clients from accessing the Redis instance."
    },
    "authorized_network": {
      "description": "The full name of the Google Compute Engine network to which the\ninstance is connected. If left unspecified, the default network\nwill be used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "References a VPC network used by the Redis instance. The network name or ID is an external resource reference, so it is out of scope for policy enforcement and should not be constrained here."
    },
    "connect_mode": {
      "description": "The connection mode of the Redis instance. Default value: \"DIRECT_PEERING\" Possible values: [\"DIRECT_PEERING\", \"PRIVATE_SERVICE_ACCESS\"]",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Controls how the Redis instance connects to the VPC network. Policy can require an approved private connectivity mode to reduce unnecessary network exposure."
    },
    "customer_managed_key": {
      "description": "Optional. The KMS key reference that you want to use to encrypt the data at rest for this Redis\ninstance. If this is provided, CMEK is enabled.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Configures a customer-managed encryption key for data at rest. Policy can require a non-empty CMEK configuration without hardcoding a specific key."
    },
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Controls whether Terraform can delete the Redis instance. Preventing unintended deletion helps protect the resource and its stored data."
    },
    "deletion_protection": {
      "description": "Whether Terraform will be prevented from destroying the instance.\nWhen a'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is not set to false in Terraform state.\nWhen the field is set to true or unset in Terraform state, a 'terraform apply'\nor 'terraform destroy' that would delete the instance will fail.\nWhen the field is set to false, deleting the instance is allowed.",
      "required": false,
      "type": "bool",
      "security_impact": true,
      "rationale": "Controls whether deletion protection is enabled. Enabling it helps prevent accidental or unauthorized destruction of the Redis instance."
    },
    "display_name": {
      "description": "An arbitrary and optional user-provided name for the instance.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Provides a user-defined display name for the instance and does not directly affect its security configuration."
    },
    "labels": {
      "description": "Resource labels to represent user provided metadata.\n\n**Note**: This field is non-authoritative, and will only manage the labels present in your configuration.\nPlease refer to the field 'effective_labels' for all of the labels present on the resource.",
      "required": false,
      "type": "map(string)",
      "security_impact": false,
      "rationale": "Provides metadata for organising and identifying resources and does not directly control the security of the Redis instance."
    },
    "location_id": {
      "description": "The zone where the instance will be provisioned. If not provided,\nthe service will choose a zone for the instance. For STANDARD_HA tier,\ninstances will be created across two zones for protection against\nzonal failures. If [alternativeLocationId] is also provided, it must\nbe different from [locationId].",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Controls the deployment location of the Redis instance. This is security-relevant because approved regions may be required for data residency and organisational deployment requirements, so the value should be restricted to approved locations."
    },
    "maintenance_version": {
      "description": "The self service update maintenance version.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Specifies the self-service maintenance version and is primarily an operational maintenance setting rather than a platform security control."
    },
    "memory_size_gb": {
      "description": "Redis memory size in GiB.",
      "required": true,
      "type": "number",
      "security_impact": false,
      "rationale": "Defines the amount of memory allocated to Redis and is a capacity and performance setting rather than a security control."
    },
    "name": {
      "description": "The ID of the instance or a fully qualified identifier for the instance.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Identifies the Redis instance and does not directly affect its security configuration."
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Identifies the Google Cloud project containing the resource. It is a resource identifier/reference and is not a platform security policy target."
    },
    "read_replicas_mode": {
      "description": "Optional. Read replica mode. Can only be specified when trying to create the instance.\nIf not set, Memorystore Redis backend will default to READ_REPLICAS_DISABLED.\n- READ_REPLICAS_DISABLED: If disabled, read endpoint will not be provided and the\ninstance cannot scale up or down the number of replicas.\n- READ_REPLICAS_ENABLED: If enabled, read endpoint will be provided and the instance\ncan scale up and down the number of replicas. Possible values: [\"READ_REPLICAS_DISABLED\", \"READ_REPLICAS_ENABLED\"]",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Controls whether read replicas are enabled and primarily affects scalability and availability rather than security."
    },
    "redis_configs": {
      "description": "Redis configuration parameters, according to http://redis.io/topics/config.\nPlease check Memorystore documentation for the list of supported parameters:\nhttps://cloud.google.com/memorystore/docs/redis/reference/rest/v1/projects.locations.instances#Instance.FIELDS.redis_configs",
      "required": false,
      "type": "map(string)",
      "security_impact": true,
      "rationale": "Controls Redis configuration parameters that can affect the security posture of the instance. Policies can restrict insecure settings, such as configuration that enables dangerous commands or weakens authentication or transport-related protections."
    },
    "redis_version": {
      "description": "The version of Redis software. If not provided, latest supported\nversion will be used. Please check the API documentation linked\nat the top for the latest valid values.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Selects the Redis software engine version. Supported versions are managed by the cloud provider, and version selection is treated as an operational or team choice unless a specific documented-insecure version must be prohibited."
    },
    "region": {
      "description": "The name of the Redis region of the instance.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Determines the region/zone where the resource is created. Policy should restrict it to an approved-region whitelist for data residency; the allowed set is parameterised (a hardcoded example whitelist is acceptable)."
    },
    "replica_count": {
      "description": "Optional. The number of replica nodes. The valid range for the Standard Tier with\nread replicas enabled is [1-5] and defaults to 2. If read replicas are not enabled\nfor a Standard Tier instance, the only valid value is 1 and the default is 1.\nThe valid value for basic tier is 0 and the default is also 0.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "Determines the number of replica nodes and primarily affects scalability and availability rather than security."
    },
    "reserved_ip_range": {
      "description": "The CIDR range of internal addresses that are reserved for this\ninstance. If not provided, the service will choose an unused /29\nblock, for example, 10.0.0.0/29 or 192.168.0.0/29. Ranges must be\nunique and non-overlapping with existing subnets in an authorized\nnetwork.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Specifies the internal IP range reserved for the instance and is primarily a network addressing configuration rather than a generic security policy target."
    },
    "secondary_ip_range": {
      "description": "Optional. Additional IP range for node placement. Required when enabling read replicas on\nan existing instance. For DIRECT_PEERING mode value must be a CIDR range of size /28, or\n\"auto\". For PRIVATE_SERVICE_ACCESS mode value must be the name of an allocated address\nrange associated with the private service access connection, or \"auto\".",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Specifies an additional internal IP range for node placement and is primarily a network addressing and scaling configuration."
    },
    "tier": {
      "description": "The service tier of the instance. Must be one of these values:\n\n- BASIC: standalone instance\n- STANDARD_HA: highly available primary/replica instances Default value: \"BASIC\" Possible values: [\"BASIC\", \"STANDARD_HA\"]",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Determines whether the instance uses Basic or Standard HA architecture and primarily affects availability and resilience rather than security."
    },
    "transit_encryption_mode": {
      "description": "The TLS mode of the Redis instance, If not provided, TLS is disabled for the instance.\n\n- SERVER_AUTHENTICATION: Client to Server traffic encryption enabled with server authentication Default value: \"DISABLED\" Possible values: [\"SERVER_AUTHENTICATION\", \"DISABLED\"]",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Controls TLS encryption for client-to-server traffic. Policy should require encrypted transit to protect Redis data and credentials from interception."
    },
    "maintenance_policy": {
      "type": "block",
      "description": "Maintenance policy for an instance.",
      "required": false
    },
    "maintenance_policy.description": {
      "description": "Optional. Description of what this policy is for.\nCreate/Update methods return INVALID_ARGUMENT if the\nlength is greater than 512.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Provides descriptive information about the maintenance policy and does not affect security."
    },
    "maintenance_policy.weekly_maintenance_window": {
      "type": "block",
      "description": "Optional. Maintenance window that is applied to resources covered by this policy.\nMinimum 1. For the current version, the maximum number\nof weekly_window is expected to be one.",
      "required": false
    },
    "maintenance_policy.weekly_maintenance_window.day": {
      "description": "Required. The day of week that maintenance updates occur.\n\n- DAY_OF_WEEK_UNSPECIFIED: The day of the week is unspecified.\n- MONDAY: Monday\n- TUESDAY: Tuesday\n- WEDNESDAY: Wednesday\n- THURSDAY: Thursday\n- FRIDAY: Friday\n- SATURDAY: Saturday\n- SUNDAY: Sunday Possible values: [\"DAY_OF_WEEK_UNSPECIFIED\", \"MONDAY\", \"TUESDAY\", \"WEDNESDAY\", \"THURSDAY\", \"FRIDAY\", \"SATURDAY\", \"SUNDAY\"]",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Specifies when maintenance occurs and is an operational scheduling setting rather than a security control."
    },
    "maintenance_policy.weekly_maintenance_window.start_time": {
      "type": "block",
      "description": "Required. Start time of the window in UTC time.",
      "required": true
    },
    "maintenance_policy.weekly_maintenance_window.start_time.hours": {
      "description": "Hours of day in 24 hour format. Should be from 0 to 23.\nAn API may choose to allow the value \"24:00:00\" for scenarios like business closing time.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "Defines the maintenance start hour and does not directly affect resource security."
    },
    "maintenance_policy.weekly_maintenance_window.start_time.minutes": {
      "description": "Minutes of hour of day. Must be from 0 to 59.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "Defines the maintenance start minute and does not directly affect resource security."
    },
    "maintenance_policy.weekly_maintenance_window.start_time.nanos": {
      "description": "Fractions of seconds in nanoseconds. Must be from 0 to 999,999,999.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "Defines fractional seconds for the maintenance start time and does not affect security."
    },
    "maintenance_policy.weekly_maintenance_window.start_time.seconds": {
      "description": "Seconds of minutes of the time. Must normally be from 0 to 59.\nAn API may allow the value 60 if it allows leap-seconds.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "Defines seconds for the maintenance start time and does not directly affect security."
    },
    "persistence_config": {
      "type": "block",
      "description": "Persistence configuration for an instance.",
      "required": false
    },
    "persistence_config.persistence_mode": {
      "description": "Optional. Controls whether Persistence features are enabled. If not provided, the existing value will be used.\n\n- DISABLED: \tPersistence is disabled for the instance, and any existing snapshots are deleted.\n- RDB: RDB based Persistence is enabled. Possible values: [\"DISABLED\", \"RDB\"]",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Controls whether Redis persistence is enabled and is primarily a data durability and recovery configuration rather than a platform security control."
    },
    "persistence_config.rdb_snapshot_period": {
      "description": "Optional. Available snapshot periods for scheduling.\n\n- ONE_HOUR:\tSnapshot every 1 hour.\n- SIX_HOURS:\tSnapshot every 6 hours.\n- TWELVE_HOURS:\tSnapshot every 12 hours.\n- TWENTY_FOUR_HOURS:\tSnapshot every 24 hours. Possible values: [\"ONE_HOUR\", \"SIX_HOURS\", \"TWELVE_HOURS\", \"TWENTY_FOUR_HOURS\"]",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Determines how frequently persistence snapshots are created and is primarily an operational backup scheduling setting."
    },
    "persistence_config.rdb_snapshot_start_time": {
      "description": "Optional. Date and time that the first snapshot was/will be attempted,\nand to which future snapshots will be aligned. If not provided,\nthe current time will be used.\nA timestamp in RFC3339 UTC \"Zulu\" format, with nanosecond resolution\nand up to nine fractional digits.\nExamples: \"2014-10-02T15:01:23Z\" and \"2014-10-02T15:01:23.045123456Z\".",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Specifies when scheduled persistence snapshots begin and is an operational scheduling setting rather than a security control."
    }
  }
}
