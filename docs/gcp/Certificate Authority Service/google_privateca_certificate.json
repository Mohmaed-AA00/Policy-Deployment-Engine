{
  "last_updated": "2026-06-23T08:26:43Z",
  "provider_version": "7.37.0",
  "arguments": {
    "certificate_authority": {
      "description": "The Certificate Authority ID that should issue the certificate. For example, to issue a Certificate from\na Certificate Authority with resource name 'projects/my-project/locations/us-central1/caPools/my-pool/certificateAuthorities/my-ca',\nargument 'pool' should be set to 'projects/my-project/locations/us-central1/caPools/my-pool', argument 'certificate_authority'\nshould be set to 'my-ca'.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Reference to the issuing CA; does not control a security-configurable property of the certificate itself."
    },
    "certificate_template": {
      "description": "The resource name for a CertificateTemplate used to issue this certificate,\nin the format 'projects/*/locations/*/certificateTemplates/*'. If this is specified,\nthe caller must have the necessary permission to use this template. If this is\nomitted, no template will be used. This template must be in the same location\nas the Certificate.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Optional reference to a CertificateTemplate; the template resource itself is evaluated separately."
    },
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Preventing deletion protects the managed certificate from accidental destruction. Because certificates are part of the platform's security and trust infrastructure, requiring a safe deletion policy reduces the risk of losing a critical security resource."
    },
    "labels": {
      "description": "Labels with user-defined metadata to apply to this resource.\n\n\n**Note**: This field is non-authoritative, and will only manage the labels present in your configuration.\nPlease refer to the field 'effective_labels' for all of the labels present on the resource.",
      "required": false,
      "type": "map(string)",
      "security_impact": false,
      "rationale": "User-defined metadata; no security impact."
    },
    "lifetime": {
      "description": "The desired lifetime of the CA certificate. Used to create the \"notBeforeTime\" and\n\"notAfterTime\" fields inside an X.509 certificate. A duration in seconds with up to nine\nfractional digits, terminated by 's'. Example: \"3.5s\".",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Certificate validity period; no specific policy boundary is mandated for individual certificates issued by this resource."
    },
    "location": {
      "description": "Location of the Certificate. A full list of valid locations can be found by\nrunning 'gcloud privateca locations list'.",
      "required": true,
      "type": "string",
      "security_impact": true,
      "rationale": "Determines the region/zone where the resource is created. Policy should restrict it to an approved-region whitelist for data residency; the allowed set is parameterised (a hardcoded example whitelist is acceptable)."
    },
    "name": {
      "description": "The name for this Certificate.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Resource identifier only; no security-relevant configuration."
    },
    "pem_csr": {
      "description": "Immutable. A pem-encoded X.509 certificate signing request (CSR).",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Raw CSR content; policy cannot enforce constraints on arbitrary CSR data."
    },
    "pool": {
      "description": "The name of the CaPool this Certificate belongs to.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Resource reference only; does not configure a security-enforceable property."
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "GCP project reference; no security-enforceable constraint."
    },
    "config": {
      "type": "block",
      "description": "The config used to create a self-signed X.509 certificate or CSR.",
      "required": false
    },
    "config.public_key": {
      "type": "block",
      "description": "A PublicKey describes a public key.",
      "required": true
    },
    "config.public_key.format": {
      "description": "The format of the public key. Currently, only PEM format is supported. Possible values: [\"KEY_TYPE_UNSPECIFIED\", \"PEM\"]",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Public key format; only PEM is currently supported by the provider so no enforcement is needed."
    },
    "config.public_key.key": {
      "description": "Required. A public key. When this is specified in a request, the padding and encoding can be any of the options described by the respective 'KeyType' value. When this is generated by the service, it will always be an RFC 5280 SubjectPublicKeyInfo structure containing an algorithm identifier and a key. A base64-encoded string.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Raw public key material; policy cannot enforce constraints on the key content itself."
    },
    "config.subject_config": {
      "type": "block",
      "description": "Specifies some of the values in a certificate that are related to the subject.",
      "required": true
    },
    "config.subject_config.subject": {
      "type": "block",
      "description": "Contains distinguished name fields such as the location and organization.",
      "required": true
    },
    "config.subject_config.subject.common_name": {
      "description": "The common name of the distinguished name.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Certificate subject identity field; no security policy boundary is applicable."
    },
    "config.subject_config.subject.country_code": {
      "description": "The country code of the subject.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Certificate subject identity field; no security policy boundary is applicable."
    },
    "config.subject_config.subject.locality": {
      "description": "The locality or city of the subject.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Certificate subject identity field; no security policy boundary is applicable."
    },
    "config.subject_config.subject.organization": {
      "description": "The organization of the subject.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Certificate subject identity field; no security policy boundary is applicable."
    },
    "config.subject_config.subject.organizational_unit": {
      "description": "The organizational unit of the subject.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Certificate subject identity field; no security policy boundary is applicable."
    },
    "config.subject_config.subject.postal_code": {
      "description": "The postal code of the subject.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Certificate subject identity field; no security policy boundary is applicable."
    },
    "config.subject_config.subject.province": {
      "description": "The province, territory, or regional state of the subject.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Certificate subject identity field; no security policy boundary is applicable."
    },
    "config.subject_config.subject.street_address": {
      "description": "The street address of the subject.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Certificate subject identity field; no security policy boundary is applicable."
    },
    "config.subject_config.subject_alt_name": {
      "type": "block",
      "description": "The subject alternative name fields.",
      "required": false
    },
    "config.subject_config.subject_alt_name.dns_names": {
      "description": "Contains only valid, fully-qualified host names.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "SAN DNS names are application-specific; no enforcement boundary is mandated."
    },
    "config.subject_config.subject_alt_name.email_addresses": {
      "description": "Contains only valid RFC 2822 E-mail addresses.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "SAN email addresses are application-specific; no enforcement boundary is mandated."
    },
    "config.subject_config.subject_alt_name.ip_addresses": {
      "description": "Contains only valid 32-bit IPv4 addresses or RFC 4291 IPv6 addresses.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "SAN IP addresses are application-specific; no enforcement boundary is mandated."
    },
    "config.subject_config.subject_alt_name.uris": {
      "description": "Contains only valid RFC 3986 URIs.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "SAN URIs are application-specific; no enforcement boundary is mandated."
    },
    "config.subject_key_id": {
      "type": "block",
      "description": "When specified this provides a custom SKI to be used in the certificate. This should only be used to maintain a SKI of an existing CA originally created outside CA service, which was not generated using method (1) described in RFC 5280 section 4.2.1.2..",
      "required": false
    },
    "config.subject_key_id.key_id": {
      "description": "The value of the KeyId in lowercase hexadecimal.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Custom SKI hex value used for legacy CA migration; no enforcement boundary is mandated."
    },
    "config.x509_config": {
      "type": "block",
      "description": "Describes how some of the technical X.509 fields in a certificate should be populated.",
      "required": true
    },
    "config.x509_config.aia_ocsp_servers": {
      "description": "Describes Online Certificate Status Protocol (OCSP) endpoint addresses that appear in the\n\"Authority Information Access\" extension in the certificate.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "OCSP endpoint list is informational; no specific URL value is mandated by policy."
    },
    "config.x509_config.additional_extensions": {
      "type": "block",
      "description": "Specifies an X.509 extension, which may be used in different parts of X.509 objects like certificates, CSRs, and CRLs.",
      "required": false
    },
    "config.x509_config.additional_extensions.critical": {
      "description": "Indicates whether or not this extension is critical (i.e., if the client does not know how to\nhandle this extension, the client should consider this to be an error).",
      "required": true,
      "type": "bool",
      "security_impact": false,
      "rationale": "This argument determines whether clients must reject the certificate when they cannot process the custom X.509 extension. The appropriate setting depends on the meaning and intended enforcement of that specific extension, so a generic policy cannot require all custom extensions to be critical or non-critical."
    },
    "config.x509_config.additional_extensions.value": {
      "description": "The value of this X.509 extension. A base64-encoded string.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Raw extension value is application-specific; policy cannot enforce constraints on arbitrary extension data."
    },
    "config.x509_config.additional_extensions.object_id": {
      "type": "block",
      "description": "Describes values that are relevant in a CA certificate.",
      "required": true
    },
    "config.x509_config.additional_extensions.object_id.object_id_path": {
      "description": "An ObjectId specifies an object identifier (OID). These provide context and describe types in ASN.1 messages.",
      "required": true,
      "type": "list(number)",
      "security_impact": false,
      "rationale": "This argument identifies the type and meaning of a custom X.509 extension using an object identifier path. The correct OID depends on the specific extension and its application-defined purpose, so a generic policy cannot prescribe one universally safe value."
    },
    "config.x509_config.ca_options": {
      "type": "block",
      "description": "Describes values that are relevant in a CA certificate.",
      "required": false
    },
    "config.x509_config.ca_options.is_ca": {
      "description": "When true, the \"CA\" in Basic Constraints extension will be set to true.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Controls CA flag in the Basic Constraints extension; appropriate value depends on the certificate's intended use case."
    },
    "config.x509_config.ca_options.max_issuer_path_length": {
      "description": "Refers to the \"path length constraint\" in Basic Constraints extension. For a CA certificate, this value describes the depth of\nsubordinate CA certificates that are allowed. If this value is less than 0, the request will fail.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "Path length constraint is specific to the CA hierarchy depth; no universal enforcement boundary is mandated."
    },
    "config.x509_config.ca_options.non_ca": {
      "description": "When true, the \"CA\" in Basic Constraints extension will be set to false.\nIf both 'is_ca' and 'non_ca' are unset, the extension will be omitted from the CA certificate.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Explicitly sets the CA flag to false in Basic Constraints; appropriate value depends on the certificate's intended use case."
    },
    "config.x509_config.ca_options.zero_max_issuer_path_length": {
      "description": "When true, the \"path length constraint\" in Basic Constraints extension will be set to 0.\nif both 'max_issuer_path_length' and 'zero_max_issuer_path_length' are unset,\nthe max path length will be omitted from the CA certificate.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Convenience flag to set path length to 0; appropriate value depends on the CA hierarchy design."
    },
    "config.x509_config.key_usage": {
      "type": "block",
      "description": "Indicates the intended use for keys that correspond to a certificate.",
      "required": true
    },
    "config.x509_config.key_usage.base_key_usage": {
      "type": "block",
      "description": "Describes high-level ways in which a key may be used.",
      "required": true
    },
    "config.x509_config.key_usage.base_key_usage.cert_sign": {
      "description": "The key may be used to sign certificates.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Key capability flag; the combination of key usages is application-specific and no specific setting is mandated."
    },
    "config.x509_config.key_usage.base_key_usage.content_commitment": {
      "description": "The key may be used for cryptographic commitments. Note that this may also be referred to as \"non-repudiation\".",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Key capability flag; the combination of key usages is application-specific and no specific setting is mandated."
    },
    "config.x509_config.key_usage.base_key_usage.crl_sign": {
      "description": "The key may be used sign certificate revocation lists.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Key capability flag; the combination of key usages is application-specific and no specific setting is mandated."
    },
    "config.x509_config.key_usage.base_key_usage.data_encipherment": {
      "description": "The key may be used to encipher data.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Key capability flag; the combination of key usages is application-specific and no specific setting is mandated."
    },
    "config.x509_config.key_usage.base_key_usage.decipher_only": {
      "description": "The key may be used to decipher only.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Key capability flag; the combination of key usages is application-specific and no specific setting is mandated."
    },
    "config.x509_config.key_usage.base_key_usage.digital_signature": {
      "description": "The key may be used for digital signatures.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Key capability flag; the combination of key usages is application-specific and no specific setting is mandated."
    },
    "config.x509_config.key_usage.base_key_usage.encipher_only": {
      "description": "The key may be used to encipher only.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Key capability flag; the combination of key usages is application-specific and no specific setting is mandated."
    },
    "config.x509_config.key_usage.base_key_usage.key_agreement": {
      "description": "The key may be used in a key agreement protocol.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Key capability flag; the combination of key usages is application-specific and no specific setting is mandated."
    },
    "config.x509_config.key_usage.base_key_usage.key_encipherment": {
      "description": "The key may be used to encipher other keys.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Key capability flag; the combination of key usages is application-specific and no specific setting is mandated."
    },
    "config.x509_config.key_usage.extended_key_usage": {
      "type": "block",
      "description": "Describes high-level ways in which a key may be used.",
      "required": true
    },
    "config.x509_config.key_usage.extended_key_usage.client_auth": {
      "description": "Corresponds to OID 1.3.6.1.5.5.7.3.2. Officially described as \"TLS WWW client authentication\", though regularly used for non-WWW TLS.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Extended key usage flag; the appropriate EKU combination is use-case specific and no specific setting is mandated."
    },
    "config.x509_config.key_usage.extended_key_usage.code_signing": {
      "description": "Corresponds to OID 1.3.6.1.5.5.7.3.3. Officially described as \"Signing of downloadable executable code client authentication\".",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Extended key usage flag; the appropriate EKU combination is use-case specific and no specific setting is mandated."
    },
    "config.x509_config.key_usage.extended_key_usage.email_protection": {
      "description": "Corresponds to OID 1.3.6.1.5.5.7.3.4. Officially described as \"Email protection\".",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Extended key usage flag; the appropriate EKU combination is use-case specific and no specific setting is mandated."
    },
    "config.x509_config.key_usage.extended_key_usage.ocsp_signing": {
      "description": "Corresponds to OID 1.3.6.1.5.5.7.3.9. Officially described as \"Signing OCSP responses\".",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Extended key usage flag; the appropriate EKU combination is use-case specific and no specific setting is mandated."
    },
    "config.x509_config.key_usage.extended_key_usage.server_auth": {
      "description": "Corresponds to OID 1.3.6.1.5.5.7.3.1. Officially described as \"TLS WWW server authentication\", though regularly used for non-WWW TLS.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Extended key usage flag; the appropriate EKU combination is use-case specific and no specific setting is mandated."
    },
    "config.x509_config.key_usage.extended_key_usage.time_stamping": {
      "description": "Corresponds to OID 1.3.6.1.5.5.7.3.8. Officially described as \"Binding the hash of an object to a time\".",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Extended key usage flag; the appropriate EKU combination is use-case specific and no specific setting is mandated."
    },
    "config.x509_config.key_usage.unknown_extended_key_usages": {
      "type": "block",
      "description": "An ObjectId specifies an object identifier (OID). These provide context and describe types in ASN.1 messages.",
      "required": false
    },
    "config.x509_config.key_usage.unknown_extended_key_usages.object_id_path": {
      "description": "An ObjectId specifies an object identifier (OID). These provide context and describe types in ASN.1 messages.",
      "required": true,
      "type": "list(number)",
      "security_impact": false,
      "rationale": "This argument specifies a custom extended key usage through an object identifier path. The correct OID depends on the application, protocol and intended certificate purpose, so a generic policy cannot prescribe a universally appropriate value."
    },
    "config.x509_config.name_constraints": {
      "type": "block",
      "description": "Describes the X.509 name constraints extension.",
      "required": false
    },
    "config.x509_config.name_constraints.critical": {
      "description": "Indicates whether or not the name constraints are marked critical.",
      "required": true,
      "type": "bool",
      "security_impact": true,
      "rationale": "This argument determines whether clients must reject a CA certificate when they cannot process its name constraints. A generic policy can require the extension to be critical so namespace restrictions cannot be silently ignored during certificate-path validation, consistent with RFC 5280."
    },
    "config.x509_config.name_constraints.excluded_dns_names": {
      "description": "Contains excluded DNS names. Any DNS name that can be\nconstructed by simply adding zero or more labels to\nthe left-hand side of the name satisfies the name constraint.\nFor example, 'example.com', 'www.example.com', 'www.sub.example.com'\nwould satisfy 'example.com' while 'example1.com' does not.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Excluded DNS name constraints are PKI-design specific; no specific values are mandated by policy."
    },
    "config.x509_config.name_constraints.excluded_email_addresses": {
      "description": "Contains the excluded email addresses. The value can be a particular\nemail address, a hostname to indicate all email addresses on that host or\na domain with a leading period (e.g. '.example.com') to indicate\nall email addresses in that domain.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Excluded email address constraints are PKI-design specific; no specific values are mandated by policy."
    },
    "config.x509_config.name_constraints.excluded_ip_ranges": {
      "description": "Contains the excluded IP ranges. For IPv4 addresses, the ranges\nare expressed using CIDR notation as specified in RFC 4632.\nFor IPv6 addresses, the ranges are expressed in similar encoding as IPv4\naddresses.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Excluded IP range constraints are PKI-design specific; no specific values are mandated by policy."
    },
    "config.x509_config.name_constraints.excluded_uris": {
      "description": "Contains the excluded URIs that apply to the host part of the name.\nThe value can be a hostname or a domain with a\nleading period (like '.example.com')",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Excluded URI constraints are PKI-design specific; no specific values are mandated by policy."
    },
    "config.x509_config.name_constraints.permitted_dns_names": {
      "description": "Contains permitted DNS names. Any DNS name that can be\nconstructed by simply adding zero or more labels to\nthe left-hand side of the name satisfies the name constraint.\nFor example, 'example.com', 'www.example.com', 'www.sub.example.com'\nwould satisfy 'example.com' while 'example1.com' does not.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Permitted DNS name constraints are PKI-design specific; no specific values are mandated by policy."
    },
    "config.x509_config.name_constraints.permitted_email_addresses": {
      "description": "Contains the permitted email addresses. The value can be a particular\nemail address, a hostname to indicate all email addresses on that host or\na domain with a leading period (e.g. '.example.com') to indicate\nall email addresses in that domain.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Permitted email address constraints are PKI-design specific; no specific values are mandated by policy."
    },
    "config.x509_config.name_constraints.permitted_ip_ranges": {
      "description": "Contains the permitted IP ranges. For IPv4 addresses, the ranges\nare expressed using CIDR notation as specified in RFC 4632.\nFor IPv6 addresses, the ranges are expressed in similar encoding as IPv4\naddresses.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Permitted IP range constraints are PKI-design specific; no specific values are mandated by policy."
    },
    "config.x509_config.name_constraints.permitted_uris": {
      "description": "Contains the permitted URIs that apply to the host part of the name.\nThe value can be a hostname or a domain with a\nleading period (like '.example.com')",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Permitted URI constraints are PKI-design specific; no specific values are mandated by policy."
    },
    "config.x509_config.policy_ids": {
      "type": "block",
      "description": "Describes the X.509 certificate policy object identifiers, per https://tools.ietf.org/html/rfc5280#section-4.2.1.4.",
      "required": false
    },
    "config.x509_config.policy_ids.object_id_path": {
      "description": "An ObjectId specifies an object identifier (OID). These provide context and describe types in ASN.1 messages.",
      "required": true,
      "type": "list(number)",
      "security_impact": false,
      "rationale": "This argument identifies an applicable certificate policy using an object identifier path. The correct OID depends on the organisation's certificate policy and assurance framework, so a generic policy cannot prescribe one universally appropriate value."
    }
  }
}
