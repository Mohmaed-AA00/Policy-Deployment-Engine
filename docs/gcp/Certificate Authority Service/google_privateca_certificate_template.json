{
  "last_updated": "2026-06-23T08:26:43Z",
  "provider_version": "7.37.0",
  "arguments": {
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Controls whether Terraform can destroy the certificate template. Enforcing PREVENT protects centrally defined certificate issuance constraints from accidental deletion."
    },
    "description": {
      "description": "Optional. A human-readable description of scenarios this template is intended for.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Human-readable description only; no security impact."
    },
    "labels": {
      "description": "Optional. Labels with user-defined metadata.\n\n**Note**: This field is non-authoritative, and will only manage the labels present in your configuration.\nPlease refer to the field 'effective_labels' for all of the labels present on the resource.",
      "required": false,
      "type": "map(string)",
      "security_impact": false,
      "rationale": "User-defined metadata; no security impact."
    },
    "location": {
      "description": "The location for the resource",
      "required": true,
      "type": "string",
      "security_impact": true,
      "rationale": "Determines the region/zone where the resource is created. Policy should restrict it to an approved-region whitelist for data residency; the allowed set is parameterised (a hardcoded example whitelist is acceptable)."
    },
    "maximum_lifetime": {
      "description": "Optional. The maximum lifetime allowed for all issued certificates that use this template. If the issuing CaPool's IssuancePolicy specifies a maximum lifetime the minimum of the two durations will be the maximum lifetime for issued. Note that if the issuing CertificateAuthority expires before a Certificate's requested maximum_lifetime, the effective lifetime will be explicitly truncated to match it.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Maximum certificate lifetime cap at the template level; the CA pool's issuance policy already enforces a global maximum_lifetime constraint."
    },
    "name": {
      "description": "The resource name for this CertificateTemplate in the format 'projects/*/locations/*/certificateTemplates/*'.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Resource identifier only; no security-relevant configuration."
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "GCP project reference; no security-enforceable constraint."
    },
    "identity_constraints": {
      "type": "block",
      "description": "Optional. Describes constraints on identities that may be appear in Certificates issued using this template. If this is omitted, then this template will not add restrictions on a certificate's identity.",
      "required": false
    },
    "identity_constraints.allow_subject_alt_names_passthrough": {
      "description": "Required. If this is true, the SubjectAltNames extension may be copied from a certificate request into the signed certificate. Otherwise, the requested SubjectAltNames will be discarded.",
      "required": true,
      "type": "bool",
      "security_impact": false,
      "rationale": "Controls whether SANs are passed through from the request; appropriate value depends on the PKI issuance workflow."
    },
    "identity_constraints.allow_subject_passthrough": {
      "description": "Required. If this is true, the Subject field may be copied from a certificate request into the signed certificate. Otherwise, the requested Subject will be discarded.",
      "required": true,
      "type": "bool",
      "security_impact": false,
      "rationale": "Controls whether the certificate subject is passed through from the request; appropriate value depends on the PKI issuance workflow."
    },
    "identity_constraints.cel_expression": {
      "type": "block",
      "description": "Optional. A CEL expression that may be used to validate the resolved X.509 Subject and/or Subject Alternative Name before a certificate is signed. To see the full allowed syntax and some examples, see https://cloud.google.com/certificate-authority-service/docs/using-cel",
      "required": false
    },
    "identity_constraints.cel_expression.description": {
      "description": "Optional. Description of the expression. This is a longer text which describes the expression, e.g. when hovered over it in a UI.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Human-readable explanatory text for the CEL expression; it does not change expression evaluation or certificate issuance behaviour."
    },
    "identity_constraints.cel_expression.expression": {
      "description": "Textual representation of an expression in Common Expression Language syntax.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "CEL expression content is application-specific; no specific expression value is mandated by policy."
    },
    "identity_constraints.cel_expression.location": {
      "description": "Optional. String indicating the location of the expression for error reporting, e.g. a file name and a position in the file.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Diagnostic source-location metadata used for error reporting; it does not affect CEL expression evaluation or certificate issuance controls."
    },
    "identity_constraints.cel_expression.title": {
      "description": "Optional. Title for the expression, i.e. a short string describing its purpose. This can be used e.g. in UIs which allow to enter the expression.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Human-readable label for the CEL expression; no security impact."
    },
    "passthrough_extensions": {
      "type": "block",
      "description": "Optional. Describes the set of X.509 extensions that may appear in a Certificate issued using this CertificateTemplate. If a certificate request sets extensions that don't appear in the passthrough_extensions, those extensions will be dropped. If the issuing CaPool's IssuancePolicy defines baseline_values that don't appear here, the certificate issuance request will fail. If this is omitted, then this template will not add restrictions on a certificate's X.509 extensions. These constraints do not apply to X.509 extensions set in this CertificateTemplate's predefined_values.",
      "required": false
    },
    "passthrough_extensions.known_extensions": {
      "description": "Optional. A set of named X.509 extensions. Will be combined with additional_extensions to determine the full set of X.509 extensions.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "The set of allowed named extensions is application-specific; no specific values are mandated by policy."
    },
    "passthrough_extensions.additional_extensions": {
      "type": "block",
      "description": "Optional. A set of ObjectIds identifying custom X.509 extensions. Will be combined with known_extensions to determine the full set of X.509 extensions.",
      "required": false
    },
    "passthrough_extensions.additional_extensions.object_id_path": {
      "description": "Required. The parts of an OID path. The most significant parts of the path come first.",
      "required": true,
      "type": "list(number)",
      "security_impact": false,
      "rationale": "Identifies an application-specific extension by OID. The correct OID depends on the certificate profile, so no universal platform policy value can be mandated."
    },
    "predefined_values": {
      "type": "block",
      "description": "Optional. A set of X.509 values that will be applied to all issued certificates that use this template. If the certificate request includes conflicting values for the same properties, they will be overwritten by the values defined here. If the issuing CaPool's IssuancePolicy defines conflicting baseline_values for the same properties, the certificate issuance request will fail.",
      "required": false
    },
    "predefined_values.aia_ocsp_servers": {
      "description": "Optional. Describes Online Certificate Status Protocol (OCSP) endpoint addresses that appear in the \"Authority Information Access\" extension in the certificate.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "OCSP endpoint list is informational; no specific URL value is mandated by policy."
    },
    "predefined_values.additional_extensions": {
      "type": "block",
      "description": "Optional. Describes custom X.509 extensions.",
      "required": false
    },
    "predefined_values.additional_extensions.critical": {
      "description": "Optional. Indicates whether or not this extension is critical (i.e., if the client does not know how to handle this extension, the client should consider this to be an error).",
      "required": false,
      "type": "bool",
      "security_impact": true,
      "rationale": "Controls whether clients must reject a certificate when they cannot understand its additional extension. Enforcing critical handling prevents important certificate restrictions from being silently ignored."
    },
    "predefined_values.additional_extensions.value": {
      "description": "Required. The value of this X.509 extension.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Raw extension value is application-specific; policy cannot enforce constraints on arbitrary extension data."
    },
    "predefined_values.additional_extensions.object_id": {
      "type": "block",
      "description": "Required. The OID for this X.509 extension.",
      "required": true
    },
    "predefined_values.additional_extensions.object_id.object_id_path": {
      "description": "Required. The parts of an OID path. The most significant parts of the path come first.",
      "required": true,
      "type": "list(number)",
     "security_impact": false,
     "rationale": "Identifies the type of additional certificate extension. Validating OIDs against an approved set can prevent dangerous or unauthorized custom extensions from being included in certificates."
    },
    "predefined_values.ca_options": {
      "type": "block",
      "description": "Optional. Describes options in this X509Parameters that are relevant in a CA certificate.",
      "required": false
    },
    "predefined_values.ca_options.is_ca": {
      "description": "Optional. Refers to the \"CA\" X.509 extension, which is a boolean value. When this value is true, the \"CA\" in Basic Constraints extension will be set to true.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Controls CA flag in the Basic Constraints extension; appropriate value depends on the certificate's intended use case."
    },
    "predefined_values.ca_options.max_issuer_path_length": {
      "description": "Optional. Refers to the \"path length constraint\" in Basic Constraints extension. For a CA certificate, this value describes the depth of\nsubordinate CA certificates that are allowed. If this value is less than 0, the request will fail.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "Path length constraint is specific to the CA hierarchy depth; no universal enforcement boundary is mandated."
    },
    "predefined_values.ca_options.null_ca": {
      "description": "Optional. When true, the \"CA\" in Basic Constraints extension will be set to null and omitted from the CA certificate.\nIf both 'is_ca' and 'null_ca' are unset, the \"CA\" in Basic Constraints extension will be set to false.\nNote that the behavior when 'is_ca = false' for this resource is different from the behavior in the Certificate Authority, Certificate and CaPool resources.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Omits the CA flag from Basic Constraints; appropriate value depends on the certificate's intended use case."
    },
    "predefined_values.ca_options.zero_max_issuer_path_length": {
      "description": "Optional. When true, the \"path length constraint\" in Basic Constraints extension will be set to 0.\nif both 'max_issuer_path_length' and 'zero_max_issuer_path_length' are unset,\nthe max path length will be omitted from the CA certificate.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Convenience flag to set path length to 0; appropriate value depends on the CA hierarchy design."
    },
    "predefined_values.key_usage": {
      "type": "block",
      "description": "Optional. Indicates the intended use for keys that correspond to a certificate.",
      "required": false
    },
    "predefined_values.key_usage.base_key_usage": {
      "type": "block",
      "description": "Describes high-level ways in which a key may be used.",
      "required": false
    },
    "predefined_values.key_usage.base_key_usage.cert_sign": {
      "description": "The key may be used to sign certificates.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Key capability flag; the combination of key usages is application-specific and no specific setting is mandated."
    },
    "predefined_values.key_usage.base_key_usage.content_commitment": {
      "description": "The key may be used for cryptographic commitments. Note that this may also be referred to as \"non-repudiation\".",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Key capability flag; the combination of key usages is application-specific and no specific setting is mandated."
    },
    "predefined_values.key_usage.base_key_usage.crl_sign": {
      "description": "The key may be used sign certificate revocation lists.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Key capability flag; the combination of key usages is application-specific and no specific setting is mandated."
    },
    "predefined_values.key_usage.base_key_usage.data_encipherment": {
      "description": "The key may be used to encipher data.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Key capability flag; the combination of key usages is application-specific and no specific setting is mandated."
    },
    "predefined_values.key_usage.base_key_usage.decipher_only": {
      "description": "The key may be used to decipher only.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Key capability flag; the combination of key usages is application-specific and no specific setting is mandated."
    },
    "predefined_values.key_usage.base_key_usage.digital_signature": {
      "description": "The key may be used for digital signatures.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Key capability flag; the combination of key usages is application-specific and no specific setting is mandated."
    },
    "predefined_values.key_usage.base_key_usage.encipher_only": {
      "description": "The key may be used to encipher only.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Key capability flag; the combination of key usages is application-specific and no specific setting is mandated."
    },
    "predefined_values.key_usage.base_key_usage.key_agreement": {
      "description": "The key may be used in a key agreement protocol.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Key capability flag; the combination of key usages is application-specific and no specific setting is mandated."
    },
    "predefined_values.key_usage.base_key_usage.key_encipherment": {
      "description": "The key may be used to encipher other keys.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Key capability flag; the combination of key usages is application-specific and no specific setting is mandated."
    },
    "predefined_values.key_usage.extended_key_usage": {
      "type": "block",
      "description": "Detailed scenarios in which a key may be used.",
      "required": false
    },
    "predefined_values.key_usage.extended_key_usage.client_auth": {
      "description": "Corresponds to OID 1.3.6.1.5.5.7.3.2. Officially described as \"TLS WWW client authentication\", though regularly used for non-WWW TLS.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Extended key usage flag; the appropriate EKU combination is use-case specific and no specific setting is mandated."
    },
    "predefined_values.key_usage.extended_key_usage.code_signing": {
      "description": "Corresponds to OID 1.3.6.1.5.5.7.3.3. Officially described as \"Signing of downloadable executable code client authentication\".",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Extended key usage flag; the appropriate EKU combination is use-case specific and no specific setting is mandated."
    },
    "predefined_values.key_usage.extended_key_usage.email_protection": {
      "description": "Corresponds to OID 1.3.6.1.5.5.7.3.4. Officially described as \"Email protection\".",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Extended key usage flag; the appropriate EKU combination is use-case specific and no specific setting is mandated."
    },
    "predefined_values.key_usage.extended_key_usage.ocsp_signing": {
      "description": "Corresponds to OID 1.3.6.1.5.5.7.3.9. Officially described as \"Signing OCSP responses\".",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Extended key usage flag; the appropriate EKU combination is use-case specific and no specific setting is mandated."
    },
    "predefined_values.key_usage.extended_key_usage.server_auth": {
      "description": "Corresponds to OID 1.3.6.1.5.5.7.3.1. Officially described as \"TLS WWW server authentication\", though regularly used for non-WWW TLS.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Extended key usage flag; the appropriate EKU combination is use-case specific and no specific setting is mandated."
    },
    "predefined_values.key_usage.extended_key_usage.time_stamping": {
      "description": "Corresponds to OID 1.3.6.1.5.5.7.3.8. Officially described as \"Binding the hash of an object to a time\".",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Extended key usage flag; the appropriate EKU combination is use-case specific and no specific setting is mandated."
    },
    "predefined_values.key_usage.unknown_extended_key_usages": {
      "type": "block",
      "description": "Used to describe extended key usages that are not listed in the KeyUsage.ExtendedKeyUsageOptions message.",
      "required": false
    },
    "predefined_values.key_usage.unknown_extended_key_usages.object_id_path": {
      "description": "Required. The parts of an OID path. The most significant parts of the path come first.",
      "required": true,
      "type": "list(number)",
      "security_impact": false,
      "rationale": "Specifies a custom extended key usage through an OID path. The appropriate OID is application-specific and cannot be universally constrained by platform policy."
    },
    "predefined_values.name_constraints": {
      "type": "block",
      "description": "Describes the X.509 name constraints extension.",
      "required": false
    },
    "predefined_values.name_constraints.critical": {
      "description": "Indicates whether or not the name constraints are marked critical.",
      "required": true,
      "type": "bool",
      "security_impact": true,
"rationale": "Controls whether clients must reject certificates when they cannot process the template's name constraints. Marking the constraints critical prevents issuance-scope restrictions from being silently ignored."
    },
    "predefined_values.name_constraints.excluded_dns_names": {
      "description": "Contains excluded DNS names. Any DNS name that can be\nconstructed by simply adding zero or more labels to\nthe left-hand side of the name satisfies the name constraint.\nFor example, 'example.com', 'www.example.com', 'www.sub.example.com'\nwould satisfy 'example.com' while 'example1.com' does not.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Excluded DNS name constraints are PKI-design specific; no specific values are mandated by policy."
    },
    "predefined_values.name_constraints.excluded_email_addresses": {
      "description": "Contains the excluded email addresses. The value can be a particular\nemail address, a hostname to indicate all email addresses on that host or\na domain with a leading period (e.g. '.example.com') to indicate\nall email addresses in that domain.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Excluded email address constraints are PKI-design specific; no specific values are mandated by policy."
    },
    "predefined_values.name_constraints.excluded_ip_ranges": {
      "description": "Contains the excluded IP ranges. For IPv4 addresses, the ranges\nare expressed using CIDR notation as specified in RFC 4632.\nFor IPv6 addresses, the ranges are expressed in similar encoding as IPv4\naddresses.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Excluded IP range constraints are PKI-design specific; no specific values are mandated by policy."
    },
    "predefined_values.name_constraints.excluded_uris": {
      "description": "Contains the excluded URIs that apply to the host part of the name.\nThe value can be a hostname or a domain with a\nleading period (like '.example.com')",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Excluded URI constraints are PKI-design specific; no specific values are mandated by policy."
    },
    "predefined_values.name_constraints.permitted_dns_names": {
      "description": "Contains permitted DNS names. Any DNS name that can be\nconstructed by simply adding zero or more labels to\nthe left-hand side of the name satisfies the name constraint.\nFor example, 'example.com', 'www.example.com', 'www.sub.example.com'\nwould satisfy 'example.com' while 'example1.com' does not.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Permitted DNS name constraints are PKI-design specific; no specific values are mandated by policy."
    },
    "predefined_values.name_constraints.permitted_email_addresses": {
      "description": "Contains the permitted email addresses. The value can be a particular\nemail address, a hostname to indicate all email addresses on that host or\na domain with a leading period (e.g. '.example.com') to indicate\nall email addresses in that domain.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Permitted email address constraints are PKI-design specific; no specific values are mandated by policy."
    },
    "predefined_values.name_constraints.permitted_ip_ranges": {
      "description": "Contains the permitted IP ranges. For IPv4 addresses, the ranges\nare expressed using CIDR notation as specified in RFC 4632.\nFor IPv6 addresses, the ranges are expressed in similar encoding as IPv4\naddresses.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Permitted IP range constraints are PKI-design specific; no specific values are mandated by policy."
    },
    "predefined_values.name_constraints.permitted_uris": {
      "description": "Contains the permitted URIs that apply to the host part of the name.\nThe value can be a hostname or a domain with a\nleading period (like '.example.com')",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Permitted URI constraints are PKI-design specific; no specific values are mandated by policy."
    },
    "predefined_values.policy_ids": {
      "type": "block",
      "description": "Optional. Describes the X.509 certificate policy object identifiers, per https://tools.ietf.org/html/rfc5280#section-4.2.1.4.",
      "required": false
    },
    "predefined_values.policy_ids.object_id_path": {
      "description": "Required. The parts of an OID path. The most significant parts of the path come first.",
      "required": true,
      "type": "list(number)",
      "security_impact": false,
      "rationale": "Identifies an applicable certificate policy through an OID path. The correct OID depends on the organisation's PKI policy and cannot be universally mandated."
    }
  }
}
