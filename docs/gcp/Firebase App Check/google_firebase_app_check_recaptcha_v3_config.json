{
  "last_updated": "2026-06-23T08:26:44Z",
  "provider_version": "7.37.0",
  "arguments": {
    "app_id": {
      "description": "The ID of an\n[Web App](https://firebase.google.com/docs/reference/firebase-management/rest/v1beta1/projects.webApps#WebApp.FIELDS.app_id).",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "The app_id is an identifier that scopes the config to a specific app; it does not control access, encryption, or security settings."
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Specifies the GCP project context; does not independently control access or security settings."
    },
    "site_secret": {
      "description": "The site secret used to identify your service for reCAPTCHA v3 verification.\nFor security reasons, this field will never be populated in any response.",
      "required": true,
      "type": "string",
      "security_impact": true,
      "rationale": "The reCAPTCHA v3 site secret is used to authenticate your application's requests to the reCAPTCHA server. Storing this secret in Secret Manager prevents accidental exposure in source code or CI/CD logs, ensuring that only authorized services can perform verification."
    },
    "token_ttl": {
      "description": "Specifies the duration for which App Check tokens exchanged from reCAPTCHA V3 artifacts will be valid.\nIf unset, a default value of 1 hour is assumed. Must be between 30 minutes and 7 days, inclusive.\n\nA duration in seconds with up to nine fractional digits, ending with 's'. Example: \"3.5s\".",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "The Token TTL (Time To Live) defines the window of validity for an App Check token. A shorter TTL limits the replay window for intercepted tokens. 24 hours (86400s) is the maximum recommended duration to balance user experience and security."
    }
  }
}
