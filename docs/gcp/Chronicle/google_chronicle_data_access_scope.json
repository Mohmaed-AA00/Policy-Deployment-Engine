{
  "last_updated": "2026-06-23T08:26:43Z",
  "provider_version": "7.37.0",
  "arguments": {
    "allow_all": {
      "description": "Optional. Whether or not the scope allows all labels, allow_all and\nallowed_data_access_labels are mutually exclusive and one of them must be\npresent. denied_data_access_labels can still be used along with allow_all.\nWhen combined with denied_data_access_labels, access will be granted to all\ndata that doesn't have labels mentioned in denied_data_access_labels. E.g.:\nA customer with scope with denied labels A and B and allow_all will be able\nto see all data except data labeled with A and data labeled with B and data\nwith labels A and B.",
      "required": false,
      "type": "bool",
      "security_impact": true,
      "rationale": "Improper use of `allow_all = true` may result in overly permissive access to sensitive datasets. It should be used only when necessary and accompanied by denied labels to restrict risky access."
    },
    "data_access_scope_id": {
      "description": "Required. The user provided scope id which will become the last part of the name\nof the scope resource.\nNeeds to be compliant with https://google.aip.dev/122",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Identifier for the data-access scope resource. We do not constrain resource identifiers, so it is not a policy target."
    },
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "description": {
      "description": "Optional. A description of the data access scope for a human reader.",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "instance": {
      "description": "The unique identifier for the Chronicle instance, which is the same as the customer ID.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "References the parent Chronicle instance the resource belongs to. This is an identifier/reference to another resource and is not a security policy target."
    },
    "location": {
      "description": "The location of the resource. This is the geographical region where the Chronicle instance resides, such as \"us\" or \"europe-west2\".",
      "required": true,
      "type": "string",
      "security_impact": true,
      "rationale": "Determines the region/zone where the resource is created. Policy should restrict it to an approved-region whitelist for data residency; the allowed set is parameterised (a hardcoded example whitelist is acceptable)."
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "allowed_data_access_labels": {
      "type": "block",
      "description": "The allowed labels for the scope. There has to be at\nleast one label allowed for the scope to be valid.\nThe logical operator for evaluation of the allowed labels is OR.\nEither allow_all or allowed_data_access_labels needs to be provided.\nE.g.: A customer with scope with allowed labels A and B will be able\nto see data with labeled with A or B or (A and B).",
      "required": false
    },
    "allowed_data_access_labels.asset_namespace": {
      "description": "The asset namespace configured in the forwarder\nof the customer's events.",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "allowed_data_access_labels.data_access_label": {
      "description": "The name of the data access label.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "References an allowed data-access label by value within the scope. This is a reference to another resource/label rather than an enforceable security setting on this resource, so it is not a policy target."
    },
    "allowed_data_access_labels.log_type": {
      "description": "The name of the log type.",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "allowed_data_access_labels.ingestion_label": {
      "type": "block",
      "description": "Representation of an ingestion label type.",
      "required": false
    },
    "allowed_data_access_labels.ingestion_label.ingestion_label_key": {
      "description": "Required. The key of the ingestion label. Always required.",
      "required": true,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "allowed_data_access_labels.ingestion_label.ingestion_label_value": {
      "description": "Optional. The value of the ingestion label. Optional. An object\nwith no provided value and some key provided would match\nagainst the given key and ANY value.",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "denied_data_access_labels": {
      "type": "block",
      "description": "Optional. The denied labels for the scope.\nThe logical operator for evaluation of the denied labels is AND.\nE.g.: A customer with scope with denied labels A and B won't be able\nto see data labeled with A and data labeled with B\nand data with labels A and B.",
      "required": false
    },
    "denied_data_access_labels.asset_namespace": {
      "description": "The asset namespace configured in the forwarder\nof the customer's events.",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "denied_data_access_labels.data_access_label": {
      "description": "The name of the data access label.",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "denied_data_access_labels.log_type": {
      "description": "The name of the log type.",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "denied_data_access_labels.ingestion_label": {
      "type": "block",
      "description": "Representation of an ingestion label type.",
      "required": false
    },
    "denied_data_access_labels.ingestion_label.ingestion_label_key": {
      "description": "Required. The key of the ingestion label. Always required.",
      "required": true,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "denied_data_access_labels.ingestion_label.ingestion_label_value": {
      "description": "Optional. The value of the ingestion label. Optional. An object\nwith no provided value and some key provided would match\nagainst the given key and ANY value.",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    }
  }
}
