{
  "last_updated": "2026-06-23T08:26:44Z",
  "provider_version": "7.37.0",
  "arguments": {
    "authority": {
      "description": "The :authority header in the gRPC request sent from Envoy to the extension service.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale":"This field identifies the authority used to address the extension service. It is an endpoint and routing reference that depends on the deployment, rather than a generic platform-level security policy target."
    },
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Controls whether Terraform can destroy the resource. A generic policy can require PREVENT to reduce the risk of accidental or unauthorised deletion and protect the availability of the authorization control."
    },
    "description": {
      "description": "A human-readable description of the resource.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "This field is descriptive metadata only and does not affect the security behaviour of the AuthzExtension resource."
    },
    "fail_open": {
      "description": "Determines how the proxy behaves if the call to the extension fails or times out.\nWhen set to TRUE, request or response processing continues without error. Any subsequent extensions in the extension chain are also executed. When set to FALSE or the default setting of FALSE is used, one of the following happens:\n* If response headers have not been delivered to the downstream client, a generic 500 error is returned to the client. The error response can be tailored by configuring a custom error response in the load balancer.\n* If response headers have been delivered, then the HTTP stream to the downstream client is reset.",
      "required": false,
      "type": "bool",
      "security_impact": true,
      "rationale": "Directly determines whether requests continue when the authorization extension fails. A generic security policy can require fail_open to be false so authorization failures fail closed instead of bypassing the control."
    },
    "forward_headers": {
      "description": "List of the HTTP headers to forward to the extension (from the client). If omitted, all headers are sent. Each element is a string indicating the header name.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "This argument defines the specific client headers forwarded to the extension. The appropriate header list depends on the application, so a generic platform policy would be too specific."
    },
    "labels": {
      "description": "Set of labels associated with the AuthzExtension resource.\n\n\n**Note**: This field is non-authoritative, and will only manage the labels present in your configuration.\nPlease refer to the field 'effective_labels' for all of the labels present on the resource.",
      "required": false,
      "type": "map(string)",
      "security_impact": false,
      "rationale": "Labels are resource metadata and do not directly provide or change a security control."
    },
    "load_balancing_scheme": {
      "description": "Required when the service points to a backend service. All backend services and forwarding rules referenced by\nthis extension must share the same load balancing scheme. For more information, refer to\n[Backend services overview](https://cloud.google.com/load-balancing/docs/backend-service). Possible values: [\"INTERNAL_MANAGED\", \"EXTERNAL_MANAGED\"]",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "This argument selects the load balancing architecture used by the extension. The available values are functional deployment choices and are not inherently secure or insecure."
    },
    "location": {
      "description": "The location of the resource.",
      "required": true,
      "type": "string",
      "security_impact": true,
      "rationale": "Determines the region/zone where the resource is created. Policy should restrict it to an approved-region whitelist for data residency; the allowed set is parameterised (a hardcoded example whitelist is acceptable)."
    },
    "metadata": {
      "description": "The metadata provided here is included as part of the metadata_context (of type google.protobuf.Struct) in the ProcessingRequest message sent to the extension server. The metadata is available under the namespace com.google.authz_extension.<resourceName>. The following variables are supported in the metadata Struct:\n\n{forwarding_rule_id} - substituted with the forwarding rule's fully qualified resource name.",
      "required": false,
      "type": "map(string)",
      "security_impact": false,
      "rationale": "This field contains free-form context passed to the extension service. Its keys and values depend on the deployment, so it is not suitable as a generic platform-level policy target."
    },
    "name": {
      "description": "Identifier. Name of the AuthzExtension resource.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "This argument is only the resource identifier. The specific name does not define a generic platform-level security control."
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "This argument identifies the GCP project where the resource resides. It is a resource reference and is not treated as a generic platform-level security control."
    },
    "service": {
      "description": "The service that runs the extension.\nThe following values and formats are accepted:\n* 'iap.googleapis.com' when the policyProfile is set to REQUEST_AUTHZ\n* 'modelarmor.{{region}}.rep.googleapis.com' when the policyProfile is set to CONTENT_AUTHZ\n* A fully qualified domain name that can be resolved by the dataplane\n* Backend service resource URI of the form 'https://www.googleapis.com/compute/v1/projects/{{project}}/regions/{{region}}/backendServices/{{name}}' or 'https://www.googleapis.com/compute/v1/projects/{{project}}/global/backendServices/{{name}}}}'",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "This argument identifies or references the service that runs the extension. The correct service is deployment-specific, so it is treated as a resource or service reference rather than a generic policy target."
    },
    "timeout": {
      "description": "Specifies the timeout for each individual message on the stream. The timeout must be between 10-10000 milliseconds.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "This setting controls how long the extension waits for each message. It is an operational availability setting rather than a direct security control."
    },
    "wire_format": {
      "description": "The format of communication supported by the callout extension. Applicable only when the policyProfile is REQUEST_AUTHZ.\nThis field is supported only for regional AuthzExtension resources. If not specified, the default value\nEXT_PROC_GRPC is used. Global AuthzExtension resources use the EXT_PROC_GRPC wire format.\n\nSupported values:\n- WIRE_FORMAT_UNSPECIFIED:\n    No wire format is explicitly specified. The backend automatically\n    defaults this value to EXT_PROC_GRPC.\n- EXT_PROC_GRPC:\n    Uses Envoy's External Processing (ext_proc) gRPC API over a single\n    gRPC stream. The backend service must support HTTP/2 or H2C.\n    All supported events for a client request are sent over the same\n    gRPC stream. This is the default wire format.\n- EXT_AUTHZ_GRPC:\n    Uses Envoy's external authorization (ext_authz) gRPC API.\n    The backend service must support HTTP/2 or H2C.\n    This option is only supported for regional AuthzExtension resources. Possible values: [\"WIRE_FORMAT_UNSPECIFIED\", \"EXT_PROC_GRPC\", \"EXT_AUTHZ_GRPC\"]",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Selects the supported gRPC communication format for the extension. It primarily controls protocol compatibility and implementation behaviour rather than defining a generic platform-level security requirement."
    }
  }
}