{
  "last_updated": "2026-06-23T08:26:44Z",
  "provider_version": "7.37.0",
  "arguments": {
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "The deletion policy controls whether Terraform can destroy the multicast domain activation. Setting it to PREVENT blocks destructive deletion and protects the resource from accidental loss. DELETE permits destruction, while ABANDON removes the resource from Terraform management. This policy therefore enforces PREVENT as a destroy-blocking control to protect against accidental destructive loss."
    },
    "description": {
      "description": "An optional text description of the multicast domain activation.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Provides human-readable metadata only. It has no effect on authentication, authorization, encryption, networking, or any other security property of the resource."
    },
    "disable_placement_policy": {
      "description": "Option to allow disabling placement policy for multicast infrastructure.\nOnly applicable if the activation is for a domain associating with a multicast domain group.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Controls infrastructure placement behaviour for multicast infrastructure rather than access control or data protection. Constraining this setting would not provide a generic security improvement."
    },
    "labels": {
      "description": "Labels as key-value pairs\n\n**Note**: This field is non-authoritative, and will only manage the labels present in your configuration.\nPlease refer to the field 'effective_labels' for all of the labels present on the resource.",
      "required": false,
      "type": "map(string)",
      "security_impact": false,
      "rationale": "Labels are metadata used for organisation, automation, or billing. While useful for governance, they do not directly affect the security posture of the multicast domain activation."
    },
    "location": {
      "description": "Resource ID segment making up resource 'name'. It identifies the resource within its parent collection as described in https://google.aip.dev/122.",
      "required": true,
      "type": "string",
      "security_impact": true,
      "rationale": "Determines the region/zone where the resource is created. Policy should restrict it to an approved-region whitelist for data residency; the allowed set is parameterised (a hardcoded example whitelist is acceptable)."
    },
    "multicast_domain": {
      "description": "The resource name of the multicast domain to activate.\nUse the following format:\n'projects/*/locations/global/multicastDomains/*'.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "References an existing multicast domain resource. The appropriate domain depends on workload requirements, making this unsuitable for a generic platform-wide security policy."
    },
    "multicast_domain_activation_id": {
      "description": "A unique name for the multicast domain activation.\nThe name is restricted to letters, numbers, and hyphen, with the first\ncharacter a letter, and the last a letter or a number. The name must not\nexceed 48 characters.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Serves only as a unique resource identifier. Naming conventions may support operations but do not directly improve the security of the resource."
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Specifies the Google Cloud project containing the resource. Restricting projects requires organisation-specific knowledge rather than a reusable platform security policy."
    },
    "traffic_spec": {
      "type": "block",
      "description": "Specifies the traffic volume and multicast group scale parameters that are\nused to set up multicast infrastructure for a multicast domain in a zone.",
      "required": false
    },
    "traffic_spec.aggr_egress_pps": {
      "description": "Aggregated egress Packet-Per-Second for all multicast groups in the domain\nin this zone.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Sets the aggregate egress packet rate used for capacity planning and traffic sizing. Rate limits and throughput thresholds are operational resource-management settings, not platform security controls."
    },
    "traffic_spec.aggr_ingress_pps": {
      "description": "Aggregated ingress Packet-Per-Second for all multicast groups in the domain\nin this zone. Default to (aggregated_egress_pps /\nmax_per_group_subscribers) * 2.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Sets the aggregate ingress packet rate used to size multicast infrastructure. Traffic throughput and capacity values are team-dependent operational choices rather than security or exposure controls."
    },
    "traffic_spec.avg_packet_size": {
      "description": "Average packet size (Default to 512 bytes).",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "Defines the average packet-size estimate used for provisioning and capacity calculations. Packet sizing is an operational performance metric and is outside the scope of platform security policy."
    },
    "traffic_spec.max_per_group_ingress_pps": {
      "description": "Maximum ingress Packet-Per-Second for a single multicast group in this\nzone. Default to aggregated_ingress_pps / 2.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Sets the maximum ingress packet rate used for individual multicast-group sizing. This is a traffic-shaping and performance parameter rather than a platform security control."
    },
    "traffic_spec.max_per_group_subscribers": {
      "description": "Maximum number of subscribers for a single multicast group in this zone.\nDefault to max(50, aggregated_egress_pps / aggregated_ingress_pps).",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Sets a subscriber-count assumption for capacity and scalability planning, which is an operational choice rather than a platform security control."
    }
  }
}
