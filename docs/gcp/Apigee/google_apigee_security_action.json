{
  "last_updated": "2026-06-23T08:26:43Z",
  "provider_version": "7.37.0",
  "arguments": {
    "api_proxies": {
      "description": "If unset, this would apply to all proxies in the environment.\nIf set, this action is enforced only if at least one proxy in the repeated\nlist is deployed at the time of enforcement. If set, several restrictions are enforced on SecurityActions.\nThere can be at most 100 enabled actions with proxies set in an env.\nSeveral other restrictions apply on conditions and are detailed later.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "References the specific API proxies targeted by the security action. The appropriate proxies depend on the organisation's deployment, so a generic platform policy cannot meaningfully constrain these resource references."
    },
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Controls whether the security action can be deleted. Allowing deletion may unintentionally remove an important API traffic protection control."
    },
    "description": {
      "description": "An optional user provided description of the SecurityAction.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Provides human-readable documentation only and does not change how the security action evaluates or handles API traffic."
    },
    "env_id": {
      "description": "The Apigee environment that this security action applies to.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Identifies the deployment environment, and the correct value is organisation-specific."
    },
    "expire_time": {
      "description": "The expiration for this SecurityAction.\nUses RFC 3339, where generated output will always be Z-normalized and uses 0, 3, 6 or 9\nfractional digits. Offsets other than \"Z\" are also accepted.\nExamples: \"2014-10-02T15:01:23Z\", \"2014-10-02T15:01:23.045123456Z\" or \"2014-10-02T15:01:23+05:30\".",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Controls the operational lifecycle and expiration time of a temporary security action. The appropriate duration depends on the team's incident response and deployment requirements, so a generic platform policy cannot enforce a universally secure value."
    },
    "org_id": {
      "description": "The organization that this security action applies to.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Identifies the Apigee organisation, and the correct value is organisation-specific."
    },
    "security_action_id": {
      "description": "The ID to use for the SecurityAction, which will become the final component of the action's resource name.\nThis value should be 0-61 characters, and valid format is (^a-z?$).",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Identifies the security action resource and does not directly affect request handling."
    },
    "state": {
      "description": "Only an ENABLED SecurityAction is enforced. An ENABLED SecurityAction past its expiration time will not be enforced. Possible values: [\"ENABLED\", \"DISABLED\"]",
      "required": true,
      "type": "string",
      "security_impact": true,
      "rationale": "Directly controls whether the security action is enforced. A DISABLED action provides no protection even when its conditions are correctly configured."
    },
    "ttl": {
      "description": "The TTL for this SecurityAction.\nA duration in seconds with up to nine fractional digits, ending with 's'. Example: \"3.5s\".",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Controls the operational duration of the security action. The appropriate TTL depends on the team's incident-response, deployment, and lifecycle requirements, so a generic platform policy cannot enforce a universally secure value."
    },
    "allow": {
      "type": "block",
      "description": "Allow a request through if it matches this SecurityAction.",
      "required": false
    },
    "condition_config": {
      "type": "block",
      "description": "A valid SecurityAction must contain at least one condition.",
      "required": true
    },
    "condition_config.access_tokens": {
      "description": "A list of accessTokens. Limit 1000 per action.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Specifies application-specific access token values used to identify requests targeted by the security action. Generic platform policies do not constrain particular tokens, keys, identifiers, or other team-specific references."
    },
    "condition_config.api_keys": {
      "description": "A list of API keys. Limit 1000 per action.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Specifies application-specific API keys used to identify requests targeted by the security action. Generic platform policies do not constrain particular keys, identifiers, or other team-managed resource references."
    },
    "condition_config.api_products": {
      "description": "A list of API Products. Limit 1000 per action.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "References the application-specific API products whose traffic is targeted by the security action. The appropriate product identifiers depend on the team's API configuration, so a generic platform policy cannot meaningfully constrain them."
    },
    "condition_config.asns": {
      "description": "A list of ASN numbers to act on, e.g. 23. https://en.wikipedia.org/wiki/Autonomous_system_(Internet)\nThis uses int64 instead of uint32 because of https://linter.aip.dev/141/forbidden-types.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Specifies network identifiers used to target traffic from particular autonomous systems. The appropriate ASNs depend on the team's traffic sources and network architecture, so a generic platform policy cannot meaningfully constrain these identifiers."
    },
    "condition_config.bot_reasons": {
      "description": "A list of Bot Reasons. Current options: Flooder, Brute Guessor, Static Content Scraper,\nOAuth Abuser, Robot Abuser, TorListRule, Advanced Anomaly Detection, Advanced API Scraper,\nSearch Engine Crawlers, Public Clouds, Public Cloud AWS, Public Cloud Azure, and Public Cloud Google.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Specifies the bot-detection categories used to target traffic with the security action. The appropriate categories depend on the protected APIs and the team's application-specific threat model, so a generic platform policy cannot meaningfully constrain them."
    },
    "condition_config.developer_apps": {
      "description": "A list of developer apps. Limit 1000 per action.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "References the application-specific developer apps whose traffic is targeted by the security action. The appropriate app identifiers depend on the team's API ecosystem, so a generic platform policy cannot meaningfully constrain these resource references."
    },
    "condition_config.developers": {
      "description": "A list of developers. Limit 1000 per action.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "References the application-specific developer accounts whose traffic is targeted by the security action. The appropriate developer identifiers depend on the team's API configuration, so a generic platform policy cannot meaningfully constrain these entities."
    },
    "condition_config.http_methods": {
      "description": "Act only on particular HTTP methods. E.g. A read-only API can block POST/PUT/DELETE methods.\nAccepted values are: GET, HEAD, POST, PUT, DELETE, CONNECT, OPTIONS, TRACE and PATCH.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Specifies the HTTP methods whose requests are targeted by the security action. The appropriate methods depend on the API's intended functionality and rule design, and no standard HTTP method is inherently insecure, so a generic platform policy cannot meaningfully constrain this list."
    },
    "condition_config.ip_address_ranges": {
      "description": "A list of IP addresses. This could be either IPv4 or IPv6. Limited to 100 per action.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Specifies the application-specific IP addresses or ranges whose traffic is targeted by the security action. The appropriate values depend on the team's network architecture and threat intelligence, so a generic platform policy cannot meaningfully constrain these identifiers."
    },
    "condition_config.region_codes": {
      "description": "A list of countries/region codes to act on, e.g. US. This follows https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2.",
      "required": false,
      "type": "list(string)",
      "security_impact": true,
      "rationale": "Applies the action according to the request's geographic region. Incorrect region codes could permit traffic from restricted locations or block approved regions."
    },
    "condition_config.user_agents": {
      "description": "A list of user agents to deny. We look for exact matches. Limit 50 per action.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Specifies application-specific user-agent strings used to identify traffic targeted by the security action. The appropriate values depend on the team's clients and threat intelligence, so a generic platform policy cannot meaningfully constrain these matching criteria."
    },
    "deny": {
      "type": "block",
      "description": "Deny a request through if it matches this SecurityAction.",
      "required": false
    },
    "deny.response_code": {
      "description": "The HTTP response code if the Action = DENY.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "Controls the response code returned after denial but does not determine whether the request is blocked."
    },
    "flag": {
      "type": "block",
      "description": "Flag a request through if it matches this SecurityAction.",
      "required": false
    },
    "flag.headers": {
      "type": "block",
      "description": "A list of HTTP headers to be sent to the target in case of a FLAG SecurityAction.\nLimit 5 headers per SecurityAction.\nAt least one is mandatory.",
      "required": false
    },
    "flag.headers.name": {
      "description": "The header name to be sent to the target.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "The header name does not directly control access, and the appropriate value depends on the organisation's backend configuration and naming conventions."
    },
    "flag.headers.value": {
      "description": "The header value to be sent to the target.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "The header value does not directly control access, and the appropriate value depends on the organisation."
    }
  }
}
