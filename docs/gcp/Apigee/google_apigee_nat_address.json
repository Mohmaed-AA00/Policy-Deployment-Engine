{
  "last_updated": "2026-06-23T08:26:43Z",
  "provider_version": "7.37.0",
  "arguments": {
    "activate": {
      "description": "Flag that specifies whether the reserved NAT address should be activate.",
      "required": false,
      "type": "bool",
      "security_impact": true,
      "rationale": "Toggles whether the reserved NAT address is active for the instance's controlled egress path. A policy can require it be activated so outbound traffic uses the known static egress IP (enabling downstream allowlisting) rather than an uncontrolled path; the secure direction is to keep egress routed through the managed NAT address."
    },
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "instance_id": {
      "description": "The Apigee instance associated with the Apigee environment,\nin the format 'organizations/{{org_name}}/instances/{{instance_name}}'.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "It sets the instance association and does not have any direct security impact."
    },
    "name": {
      "description": "Resource ID of the NAT address.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "It sets the identifier of the NAT address and does not have any direct security impact."
    }
  }
}
