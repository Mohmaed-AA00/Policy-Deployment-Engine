{
  "last_updated": "2026-06-23T08:26:44Z",
  "provider_version": "7.37.0",
  "arguments": {
    "annotations": {
      "description": "Unstructured key value map that may be set by external tools to\nstore and arbitrary metadata. They are not queryable and should be\npreserved when modifying objects.\n\n**Note**: This field is non-authoritative, and will only manage the annotations present in your configuration.\nPlease refer to the field 'effective_annotations' for all of the annotations present on the resource.",
      "required": false,
      "type": "map(string)",
      "security_impact": false,
      "rationale": "Annotations are metadata with no direct security impact."
    },
    "app_id": {
      "description": "The [ID of a Web\nApp](https://firebase.google.com/docs/reference/firebase-management/rest/v1beta1/projects.webApps#WebApp.FIELDS.app_id)\nassociated with the backend.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "App ID is a unique identifier with no direct security implications."
    },
    "backend_id": {
      "description": "Id of the backend. Also used as the service ID for Cloud Run, and as part\nof the default domain name.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Backend ID is an identifier with no direct security impact."
    },
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "display_name": {
      "description": "Human-readable name. 63 character limit.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Display name has no security implications."
    },
    "environment": {
      "description": "The environment name of the backend, used to load environment variables\nfrom environment specific configuration.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Environment specification has no direct security impact."
    },
    "labels": {
      "description": "Unstructured key value map that can be used to organize and categorize\nobjects.\n\n**Note**: This field is non-authoritative, and will only manage the labels present in your configuration.\nPlease refer to the field 'effective_labels' for all of the labels present on the resource.",
      "required": false,
      "type": "map(string)",
      "security_impact": false,
      "rationale": "Labels are metadata with no direct security impact."
    },
    "location": {
      "description": "The canonical IDs of a Google Cloud location such as \"us-east1\".",
      "required": true,
      "type": "string",
      "security_impact": true,
      "rationale": "Determines the region/zone where the resource is created. Policy should restrict it to an approved-region whitelist for data residency; the allowed set is parameterised (a hardcoded example whitelist is acceptable)."
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Project specification uses default provider project when not specified."
    },
    "service_account": {
      "description": "The name of the service account used for Cloud Build and Cloud Run.\nShould have the role roles/firebaseapphosting.computeRunner\nor equivalent permissions.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Identifier referencing the service account the backend runs as. It is a reference to an identity whose permissions are governed by IAM, not by constraining this pointer, so it is not a policy target here."
    },
    "serving_locality": {
      "description": "Immutable. Specifies how App Hosting will serve the content for this backend. It will\neither be contained to a single region (REGIONAL_STRICT) or allowed to use\nApp Hosting's global-replicated serving infrastructure (GLOBAL_ACCESS). Possible values: [\"REGIONAL_STRICT\", \"GLOBAL_ACCESS\"]",
      "required": true,
      "type": "string",
      "security_impact": true,
      "rationale": "Controls whether the backend is pinned to a single region (REGIONAL_STRICT) or may serve globally. As a data-residency control, a policy can require regional serving so data stays within an approved region; this is the sanctioned residency archetype."
    },
    "codebase": {
      "type": "block",
      "description": "The connection to an external source repository to watch for event-driven\nupdates to the backend.",
      "required": false
    },
    "codebase.repository": {
      "description": "The resource name for the Developer Connect\n['gitRepositoryLink'](https://cloud.google.com/developer-connect/docs/api/reference/rest/v1/projects.locations.connections.gitRepositoryLinks)\nconnected to this backend, in the format:\n\nprojects/{project}/locations/{location}/connections/{connection}/gitRepositoryLinks/{repositoryLink}",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Identifier referencing the connected source repository. It is a pointer to another resource (the team's repo/connection); we don't constrain references to other resources, and a fixed format/value cannot be generically prescribed."
    },
    "codebase.root_directory": {
      "description": "If 'repository' is provided, the directory relative to the root of the\nrepository to use as the root for the deployed web app.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Root directory specification has no direct security impact."
    }
  }
}
