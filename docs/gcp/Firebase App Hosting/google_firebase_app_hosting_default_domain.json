{
  "last_updated": "2026-06-23T08:26:44Z",
  "provider_version": "7.37.0",
  "arguments": {
    "backend": {
      "description": "The ID of the Backend that this Domain is associated with",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Backend ID is a reference with no direct security implications."
    },
    "disabled": {
      "description": "Whether the domain is disabled. Defaults to false.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Domain enable/disable flag has no specific security policy."
    },
    "domain_id": {
      "description": "Id of the domain. For default domain, it should be {{backend}}--{{project_id}}.{{location}}.hosted.app",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Default domain ID follows a standard format with no specific security policy."
    },
    "location": {
      "description": "The location of the Backend that this Domain is associated with",
      "required": true,
      "type": "string",
      "security_impact": true,
      "rationale": "Determines the region/zone where the resource is created. Policy should restrict it to an approved-region whitelist for data residency; the allowed set is parameterised (a hardcoded example whitelist is acceptable)."
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Project specification uses default provider project when not specified."
    }
  }
}
