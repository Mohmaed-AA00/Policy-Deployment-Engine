{
  "last_updated": "2026-06-23T08:26:43Z",
  "provider_version": "7.37.0",
  "arguments": {
    "annotations": {
      "description": "User annotations. These attributes can only be set and used by the user, and not by Cloud Deploy. Annotations must meet the following constraints: * Annotations are key/value pairs. * Valid annotation keys have two segments: an optional prefix and name, separated by a slash ('/'). * The name segment is required and must be 63 characters or less, beginning and ending with an alphanumeric character ('[a-z0-9A-Z]') with dashes ('-'), underscores ('_'), dots ('.'), and alphanumerics between. * The prefix is optional. If specified, the prefix must be a DNS subdomain: a series of DNS labels separated by dots('.'), not longer than 253 characters in total, followed by a slash ('/'). See https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/#syntax-and-character-set for more details.\n\n**Note**: This field is non-authoritative, and will only manage the annotations present in your configuration.\nPlease refer to the field 'effective_annotations' for all of the annotations present on the resource.",
      "required": false,
      "type": "map(string)",
      "security_impact": false,
      "rationale": "Just metadata for resource organization. Doesn't affect policy enforcement behaviour."
    },
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "description": {
      "description": "Description of the 'DeployPolicy'. Max length is 255 characters.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Human-readable documentation field only. No security implications."
    },
    "labels": {
      "description": "Labels are attributes that can be set and used by both the user and by Cloud Deploy. Labels must meet the following constraints: * Keys and values can contain only lowercase letters, numeric characters, underscores, and dashes. * All characters must use UTF-8 encoding, and international characters are allowed. * Keys must start with a lowercase letter or international character. * Each resource is limited to a maximum of 64 labels. Both keys and values are additionally constrained to be <= 63 characters.\n\n**Note**: This field is non-authoritative, and will only manage the labels present in your configuration.\nPlease refer to the field 'effective_labels' for all of the labels present on the resource.",
      "required": false,
      "type": "map(string)",
      "security_impact": false,
      "rationale": "Used for billing and resource organization only. Doesn't control security enforcement."
    },
    "location": {
      "description": "The location for the resource",
      "required": true,
      "type": "string",
      "security_impact": true,
      "rationale": "Determines the region/zone where the resource is created. Policy should restrict it to an approved-region whitelist for data residency; the allowed set is parameterised (a hardcoded example whitelist is acceptable)."
    },
    "name": {
      "description": "Name of the 'DeployPolicy'.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Just a resource identifier. No security implications."
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Specifies which GCP project. Controlled by provider configuration and IAM. No security implications."
    },
    "suspended": {
      "description": "When suspended, the policy will not prevent actions from occurring, even if the action violates the policy.",
      "required": false,
      "type": "bool",
      "security_impact": true,
      "rationale": "Controls whether the deploy policy actively enforces security controls. When suspended = true, ALL deployment rules are bypassed, creating a complete security control bypass. Unauthorized suspension allows deployments without any security enforcement."
    },
    "rules": {
      "type": "block",
      "description": "Rules to apply. At least one rule must be present.",
      "required": true
    },
    "rules.rollout_restriction": {
      "type": "block",
      "description": "Rollout restrictions.",
      "required": false
    },
    "rules.rollout_restriction.actions": {
      "description": "Rollout actions to be restricted as part of the policy. If left empty, all actions will be restricted. Possible values: [\"ADVANCE\", \"APPROVE\", \"CANCEL\", \"CREATE\", \"IGNORE_JOB\", \"RETRY_JOB\", \"ROLLBACK\", \"TERMINATE_JOBRUN\"]",
      "required": false,
      "type": "list(string)",
      "security_impact": "true/false",
      "rationale": ""
    },
    "rules.rollout_restriction.id": {
      "description": "ID of the rule. This id must be unique in the 'DeployPolicy' resource to which this rule belongs. The format is 'a-z{0,62}'.",
      "required": true,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "rules.rollout_restriction.invokers": {
      "description": "What invoked the action. If left empty, all invoker types will be restricted. Possible values: [\"USER\", \"DEPLOY_AUTOMATION\"]",
      "required": false,
      "type": "list(string)",
      "security_impact": "true/false",
      "rationale": ""
    },
    "rules.rollout_restriction.time_windows": {
      "type": "block",
      "description": "Time window within which actions are restricted.",
      "required": false
    },
    "rules.rollout_restriction.time_windows.time_zone": {
      "description": "The time zone in IANA format IANA Time Zone Database (e.g. America/New_York).",
      "required": true,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "rules.rollout_restriction.time_windows.one_time_windows": {
      "type": "block",
      "description": "One-time windows within which actions are restricted.",
      "required": false
    },
    "rules.rollout_restriction.time_windows.one_time_windows.end_date": {
      "type": "block",
      "description": "End date.",
      "required": true
    },
    "rules.rollout_restriction.time_windows.one_time_windows.end_date.day": {
      "description": "Day of a month. Must be from 1 to 31 and valid for the year and month.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "rules.rollout_restriction.time_windows.one_time_windows.end_date.month": {
      "description": "Month of a year. Must be from 1 to 12.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "rules.rollout_restriction.time_windows.one_time_windows.end_date.year": {
      "description": "Year of the date. Must be from 1 to 9999.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "rules.rollout_restriction.time_windows.one_time_windows.end_time": {
      "type": "block",
      "description": "End time (exclusive). You may use 24:00 for the end of the day.",
      "required": true
    },
    "rules.rollout_restriction.time_windows.one_time_windows.end_time.hours": {
      "description": "Hours of a day in 24 hour format. Must be greater than or equal to 0 and typically must be less than or equal to 23. An API may choose to allow the value \"24:00:00\" for scenarios like business closing time.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "rules.rollout_restriction.time_windows.one_time_windows.end_time.minutes": {
      "description": "Minutes of an hour. Must be greater than or equal to 0 and less than or equal to 59.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "rules.rollout_restriction.time_windows.one_time_windows.end_time.nanos": {
      "description": "Fractions of seconds, in nanoseconds. Must be greater than or equal to 0 and less than or equal to 999,999,999.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "rules.rollout_restriction.time_windows.one_time_windows.end_time.seconds": {
      "description": "Seconds of a minute. Must be greater than or equal to 0 and typically must be less than or equal to 59. An API may allow the value 60 if it allows leap-seconds.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "rules.rollout_restriction.time_windows.one_time_windows.start_date": {
      "type": "block",
      "description": "Start date.",
      "required": true
    },
    "rules.rollout_restriction.time_windows.one_time_windows.start_date.day": {
      "description": "Day of a month. Must be from 1 to 31 and valid for the year and month, or 0 to specify a year by itself or a year and month where the day isn't significant.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "rules.rollout_restriction.time_windows.one_time_windows.start_date.month": {
      "description": "Month of a year. Must be from 1 to 12, or 0 to specify a year without a month and day.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "rules.rollout_restriction.time_windows.one_time_windows.start_date.year": {
      "description": "Year of the date. Must be from 1 to 9999, or 0 to specify a date without a year.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "rules.rollout_restriction.time_windows.one_time_windows.start_time": {
      "type": "block",
      "description": "Start time (inclusive). Use 00:00 for the beginning of the day.",
      "required": true
    },
    "rules.rollout_restriction.time_windows.one_time_windows.start_time.hours": {
      "description": "Hours of a day in 24 hour format. Must be greater than or equal to 0 and typically must be less than or equal to 23. An API may choose to allow the value \"24:00:00\" for scenarios like business closing time.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "rules.rollout_restriction.time_windows.one_time_windows.start_time.minutes": {
      "description": "Minutes of an hour. Must be greater than or equal to 0 and less than or equal to 59.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "rules.rollout_restriction.time_windows.one_time_windows.start_time.nanos": {
      "description": "Fractions of seconds, in nanoseconds. Must be greater than or equal to 0 and less than or equal to 999,999,999.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "rules.rollout_restriction.time_windows.one_time_windows.start_time.seconds": {
      "description": "Seconds of a minute. Must be greater than or equal to 0 and typically must be less than or equal to 59. An API may allow the value 60 if it allows leap-seconds.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "rules.rollout_restriction.time_windows.weekly_windows": {
      "type": "block",
      "description": "Recurring weekly windows within which actions are restricted.",
      "required": false
    },
    "rules.rollout_restriction.time_windows.weekly_windows.days_of_week": {
      "description": "Days of week. If left empty, all days of the week will be included. Possible values: [\"MONDAY\", \"TUESDAY\", \"WEDNESDAY\", \"THURSDAY\", \"FRIDAY\", \"SATURDAY\", \"SUNDAY\"]",
      "required": false,
      "type": "list(string)",
      "security_impact": "true/false",
      "rationale": ""
    },
    "rules.rollout_restriction.time_windows.weekly_windows.end_time": {
      "type": "block",
      "description": "End time (exclusive). Use 24:00 to indicate midnight. If you specify endTime you must also specify startTime. If left empty, this will block for the entire day for the days specified in daysOfWeek.",
      "required": false
    },
    "rules.rollout_restriction.time_windows.weekly_windows.end_time.hours": {
      "description": "Hours of a day in 24 hour format. Must be greater than or equal to 0 and typically must be less than or equal to 23. An API may choose to allow the value \"24:00:00\" for scenarios like business closing time.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "rules.rollout_restriction.time_windows.weekly_windows.end_time.minutes": {
      "description": "Minutes of an hour. Must be greater than or equal to 0 and less than or equal to 59.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "rules.rollout_restriction.time_windows.weekly_windows.end_time.nanos": {
      "description": "Fractions of seconds, in nanoseconds. Must be greater than or equal to 0 and less than or equal to 999,999,999.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "rules.rollout_restriction.time_windows.weekly_windows.end_time.seconds": {
      "description": "Seconds of a minute. Must be greater than or equal to 0 and typically must be less than or equal to 59. An API may allow the value 60 if it allows leap-seconds.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "rules.rollout_restriction.time_windows.weekly_windows.start_time": {
      "type": "block",
      "description": "Start time (inclusive). Use 00:00 for the beginning of the day. If you specify startTime you must also specify endTime. If left empty, this will block for the entire day for the days specified in daysOfWeek.",
      "required": false
    },
    "rules.rollout_restriction.time_windows.weekly_windows.start_time.hours": {
      "description": "Hours of a day in 24 hour format. Must be greater than or equal to 0 and typically must be less than or equal to 23. An API may choose to allow the value \"24:00:00\" for scenarios like business closing time.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "rules.rollout_restriction.time_windows.weekly_windows.start_time.minutes": {
      "description": "Minutes of an hour. Must be greater than or equal to 0 and less than or equal to 59.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "rules.rollout_restriction.time_windows.weekly_windows.start_time.nanos": {
      "description": "Fractions of seconds, in nanoseconds. Must be greater than or equal to 0 and less than or equal to 999,999,999.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "rules.rollout_restriction.time_windows.weekly_windows.start_time.seconds": {
      "description": "Seconds of a minute. Must be greater than or equal to 0 and typically must be less than or equal to 59. An API may allow the value 60 if it allows leap-seconds.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "selectors": {
      "type": "block",
      "description": "Selected resources to which the policy will be applied. At least one selector is required. If one selector matches the resource the policy applies. For example, if there are two selectors and the action being attempted matches one of them, the policy will apply to that action.",
      "required": true
    },
    "selectors.delivery_pipeline": {
      "type": "block",
      "description": "Contains attributes about a delivery pipeline.",
      "required": false
    },
    "selectors.delivery_pipeline.id": {
      "description": "ID of the DeliveryPipeline. The value of this field could be one of the following:\n- The last segment of a pipeline name\n- \"*\", all delivery pipelines in a location",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "selectors.delivery_pipeline.labels": {
      "description": "DeliveryPipeline labels.",
      "required": false,
      "type": "map(string)",
      "security_impact": "true/false",
      "rationale": ""
    },
    "selectors.target": {
      "type": "block",
      "description": "Contains attributes about a target.",
      "required": false
    },
    "selectors.target.id": {
      "description": "ID of the 'Target'. The value of this field could be one of the following: * The last segment of a target name. It only needs the ID to determine which target is being referred to * \"*\", all targets in a location.",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "selectors.target.labels": {
      "description": "Target labels.",
      "required": false,
      "type": "map(string)",
      "security_impact": "true/false",
      "rationale": ""
    }
  }
}
