{
  "last_updated": "2026-06-23T08:26:44Z",
  "provider_version": "7.37.0",
  "arguments": {
    "content_config": {
      "description": "The content config of the data store. Possible values: [\"NO_CONTENT\", \"CONTENT_REQUIRED\", \"PUBLIC_WEBSITE\"]",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Selects the kind of content the data store ingests (NO_CONTENT / CONTENT_REQUIRED / PUBLIC_WEBSITE). This is a functional data-ingestion choice tied to the team's use case, not a generic security control, so it is not a policy target."
    },
    "create_advanced_site_search": {
      "description": "If true, an advanced data store for site search will be created. If the\ndata store is not configured as site search (GENERIC vertical and\nPUBLIC_WEBSITE contentConfig), this flag will be ignored.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Toggles creation of an advanced site-search data store. This is a feature/functionality choice for the team and does not improve security if constrained, so it is not a policy target."
    },
    "data_store_id": {
      "description": "The unique id of the data store.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Identifier for the data store. We do not constrain resource identifiers, so it is not a policy target."
    },
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": " This is used to control terraform lifecycle behaviour, prevents it from destroying the data store. Setting this to PREVENT is used to safeguard against deletion of the resource, A generic policy should have this set to PREVENT"
    },
    "display_name": {
      "description": "The display name of the data store. This field must be a UTF-8 encoded\nstring with a length limit of 128 characters.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Just the name"
    },
    "industry_vertical": {
      "description": "The industry vertical that the data store registers. Possible values: [\"GENERIC\", \"MEDIA\", \"HEALTHCARE_FHIR\"]",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Selects the data store's industry vertical (GENERIC/MEDIA/HEALTHCARE_FHIR). This is a functional configuration tied to the team's data, not a security control, so it is not a policy target."
    },
    "kms_key_name": {
      "description": "KMS key resource name which will be used to encrypt resources:\n'/{project}/locations/{location}/keyRings/{keyRing}/cryptoKeys/{keyId}'\nThe KMS key to be used to protect this DataStore at creation time. Must be\nset for requests that need to comply with CMEK Org Policy protections.\nIf this field is set and processed successfully, the DataStore will be\nprotected by the KMS key, as indicated in the cmek_config field.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "KMS key used to encrypt the data store. A policy should require it be non-empty so data is always CMEK-encrypted; it must not pin a specific key."
    },
    "location": {
      "description": "The geographic location where the data store should reside. The value can\nonly be one of \"global\", \"us\" and \"eu\".",
      "required": true,
      "type": "string",
      "security_impact": true,
      "rationale": "Determines the region/zone where the resource is created. Policy should restrict it to an approved-region whitelist for data residency; the allowed set is parameterised (a hardcoded example whitelist is acceptable)."
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Project the resource belongs to. This is an identifier/reference and is not a security policy target."
    },
    "skip_default_schema_creation": {
      "description": "A boolean flag indicating whether to skip the default schema creation for\nthe data store. Only enable this flag if you are certain that the default\nschema is incompatible with your use case.\nIf set to true, you must manually create a schema for the data store\nbefore any documents can be ingested.\nThis flag cannot be specified if 'data_store.starting_schema' is\nspecified.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Toggles whether a default schema is created with the data store. This is a functional setup choice for the team and does not map to a security archetype."
    },
    "solution_types": {
      "description": "The solutions that the data store enrolls. Possible values: [\"SOLUTION_TYPE_RECOMMENDATION\", \"SOLUTION_TYPE_SEARCH\", \"SOLUTION_TYPE_CHAT\", \"SOLUTION_TYPE_GENERATIVE_CHAT\"]",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Lists which Discovery Engine solutions the data store supports. This is a functional product configuration choice, not a security control."
    },
    "advanced_site_search_config": {
      "type": "block",
      "description": "Configuration data for advance site search.",
      "required": false
    },
    "advanced_site_search_config.disable_automatic_refresh": {
      "description": "If set true, automatic refresh is disabled for the DataStore.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Controls the automatic content refresh toggle for the data store, operational scheduling choice that affects how indexed content is updated is not a security control."
    },
    "advanced_site_search_config.disable_initial_index": {
      "description": "If set true, initial indexing is disabled for the DataStore.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Controls the initial indexing being disabled when the data store is created. Operational choice that is a setup choice for the team, not a security control."
    },
    "document_processing_config": {
      "type": "block",
      "description": "Configuration for Document understanding and enrichment.",
      "required": false
    },
    "document_processing_config.chunking_config": {
      "type": "block",
      "description": "Whether chunking mode is enabled.",
      "required": false
    },
    "document_processing_config.chunking_config.layout_based_chunking_config": {
      "type": "block",
      "description": "Configuration for the layout based chunking.",
      "required": false
    },
    "document_processing_config.chunking_config.layout_based_chunking_config.chunk_size": {
      "description": "The token size limit for each chunk.\nSupported values: 100-500 (inclusive). Default value: 500.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "Setting that sets the token size limit for document chunk. This is a functional performance tuning parameter, does not affect security controls"
    },
    "document_processing_config.chunking_config.layout_based_chunking_config.include_ancestor_headings": {
      "description": "Whether to include appending different levels of headings to chunks from the middle of the document to prevent context loss.\nDefault value: False.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "This is used to control the appending of ancestor headings to preserve context, not a security control"
    },
    "document_processing_config.default_parsing_config": {
      "type": "block",
      "description": "Configurations for default Document parser. If not specified, this resource\nwill be configured to use a default DigitalParsingConfig, and the default parsing\nconfig will be applied to all file types for Document parsing.",
      "required": false
    },
    "document_processing_config.default_parsing_config.digital_parsing_config": {
      "type": "block",
      "description": "Configurations applied to digital parser.",
      "required": false
    },
    "document_processing_config.default_parsing_config.layout_parsing_config": {
      "type": "block",
      "description": "Configurations applied to layout parser.",
      "required": false
    },
    "document_processing_config.default_parsing_config.layout_parsing_config.enable_image_annotation": {
      "description": "If true, the LLM based annotation is added to the image during parsing.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Is used to toggle LLM based annotation of images when parsing, functional feature that does not affect security"
    },
    "document_processing_config.default_parsing_config.layout_parsing_config.enable_table_annotation": {
      "description": "If true, the LLM based annotation is added to the table during parsing.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Is used to toggle LLM based annotation of tables when parsing, a functional feature that does not affect security controls"
    },
    "document_processing_config.default_parsing_config.layout_parsing_config.exclude_html_classes": {
      "description": "List of HTML classes to exclude from the parsed content.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Is used to specify the HTML classes to be excluded from parsed content, filters content and is not a security control"
    },
    "document_processing_config.default_parsing_config.layout_parsing_config.exclude_html_elements": {
      "description": "List of HTML elements to exclude from the parsed content.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Filters HTML elements to exclude from the parsed content, does not affect security control"
    },
    "document_processing_config.default_parsing_config.layout_parsing_config.exclude_html_ids": {
      "description": "List of HTML ids to exclude from the parsed content.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Filters HTML IDS to exclude from the parsed content, does not affect security control"
    },
    "document_processing_config.default_parsing_config.layout_parsing_config.structured_content_types": {
      "description": "Contains the required structure types to extract from the document. Supported values: 'shareholder-structure'.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Filters the content to be extracted when being parsed,functional data-extraction choice, not a security feature"
    },
    "document_processing_config.default_parsing_config.ocr_parsing_config": {
      "type": "block",
      "description": "Configurations applied to OCR parser. Currently it only applies to PDFs.",
      "required": false
    },
    "document_processing_config.default_parsing_config.ocr_parsing_config.use_native_text": {
      "description": "If true, will use native text instead of OCR text on pages containing native text.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Controls the use of native embedded text instead of OCR extracted text in parsed PDF files, functional parsing accuracy choice does not affect security"
    },
    "document_processing_config.parsing_config_overrides": {
      "type": "block",
      "description": "Map from file type to override the default parsing configuration based on the file type. Supported keys:\n  * 'pdf': Override parsing config for PDF files, either digital parsing, ocr parsing or layout parsing is supported.\n  * 'html': Override parsing config for HTML files, only digital parsing and or layout parsing are supported.\n  * 'docx': Override parsing config for DOCX files, only digital parsing and or layout parsing are supported.",
      "required": false
    },
    "document_processing_config.parsing_config_overrides.file_type": {
      "description": "The identifier for this object. Format specified above.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "is used to identify the file type for which the configuration is being overridden, a functional config that has no security implications"
    },
    "document_processing_config.parsing_config_overrides.digital_parsing_config": {
      "type": "block",
      "description": "Configurations applied to digital parser.",
      "required": false
    },
    "document_processing_config.parsing_config_overrides.layout_parsing_config": {
      "type": "block",
      "description": "Configurations applied to layout parser.",
      "required": false
    },
    "document_processing_config.parsing_config_overrides.layout_parsing_config.enable_image_annotation": {
      "description": "If true, the LLM based annotation is added to the image during parsing.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Is used to toggle LLM based annotation of images when parsing for overrides, functional feature that does not affect security"
    }, 
    "document_processing_config.parsing_config_overrides.layout_parsing_config.enable_table_annotation": {
      "description": "If true, the LLM based annotation is added to the table during parsing.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Is used to toggle LLM based annotation of tables(for overrides) when parsing, a functional feature that does not affect security controls"
    },
    "document_processing_config.parsing_config_overrides.layout_parsing_config.exclude_html_classes": {
      "description": "List of HTML classes to exclude from the parsed content.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Filters HTML classes to exclude from the parsed content(for overrides), does not affect security contro"
    },
    "document_processing_config.parsing_config_overrides.layout_parsing_config.exclude_html_elements": {
      "description": "List of HTML elements to exclude from the parsed content.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Filters HTML elements to exclude from the parsed content(for overrides), does not affect security control"
    },
    "document_processing_config.parsing_config_overrides.layout_parsing_config.exclude_html_ids": {
      "description": "List of HTML ids to exclude from the parsed content.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Filters HTML IDS to exclude from the parsed content(for overrides), does not affect security control"
    },
    "document_processing_config.parsing_config_overrides.layout_parsing_config.structured_content_types": {
      "description": "Contains the required structure types to extract from the document. Supported values: 'shareholder-structure'.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Specifies the structured content type to extract for a specific file type override. Functional data-extraction choice tied to the teams document format."
    },
    "document_processing_config.parsing_config_overrides.ocr_parsing_config": {
      "type": "block",
      "description": "Configurations applied to OCR parser. Currently it only applies to PDFs.",
      "required": false
    },
    "document_processing_config.parsing_config_overrides.ocr_parsing_config.use_native_text": {
      "description": "If true, will use native text instead of OCR text on pages containing native text.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Controls the preference of embedded text over OCR text for a specific file type override. Functional parsing accuracy type and not a security control."
    }
  }
}
