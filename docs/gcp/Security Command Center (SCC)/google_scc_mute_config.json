{
  "last_updated": "2026-06-23T08:26:44Z",
  "provider_version": "7.37.0",
  "arguments": {
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "description": {
      "description": "A description of the mute config.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Free-text description of the mute config; cosmetic, with no security impact."
    },
    "expiry_time": {
      "description": "Optional. The expiry of the mute config. Only applicable for dynamic configs.\nIf the expiry is set, when the config expires, it is removed from all findings.\n\nA timestamp in RFC3339 UTC \"Zulu\" format, with nanosecond resolution and up to\nnine fractional digits. Examples: \"2014-10-02T15:01:23Z\" and \"2014-10-02T15:01:23.045123456Z\".",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Optional expiry of a dynamic mute config — an operational lifecycle setting, not a generic platform security control."
    },
    "filter": {
      "description": "An expression that defines the filter to apply across create/update\nevents of findings. While creating a filter string, be mindful of\nthe scope in which the mute configuration is being created. E.g.,\nIf a filter contains project = X but is created under the\nproject = Y scope, it might not match any findings.",
      "required": true,
      "type": "string",
      "security_impact": "true/false",
      "rationale": "Filters must be precise and scoped correctly to ensure only intended findings are muted, preserving visibility of genuine security risks."
    },
    "mute_config_id": {
      "description": "Unique identifier provided by the client within the parent scope.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Client-provided identifier within the parent scope; naming an identifier is not a security control."
    },
    "parent": {
      "description": "Resource name of the new mute configs's parent. Its format is\n\"organizations/[organization_id]\", \"folders/[folder_id]\", or\n\"projects/[project_id]\".",
      "required": true,
      "type": "string",
      "security_impact": "true/false",
      "rationale": "Correct parent assignment ensures mute configurations apply to the intended scope within SCC hierarchy."
    },
    "type": {
      "description": "The type of the mute config, which determines what type of mute state the config affects. Default value: \"DYNAMIC\" Possible values: [\"MUTE_CONFIG_TYPE_UNSPECIFIED\", \"STATIC\", \"DYNAMIC\"]",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": "Terraform does not expose this argument; cannot be validated through policies."
    }
  }
}
