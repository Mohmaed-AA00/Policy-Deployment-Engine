{
  "last_updated": "2026-06-23T08:26:43Z",
  "provider_version": "7.37.0",
  "arguments": {
    "disable_default_sink": {
      "description": "If set to true, the _Default sink in newly created projects and folders will created in a disabled state. This can be used to automatically disable log storage if there is already an aggregated sink configured in the hierarchy. The _Default sink can be re-enabled manually if needed.",
      "required": false,
      "type": "bool",
      "security_impact": true,
      "rationale": "Disabling the _Default sink at the organization level prevents logs from being stored in default logging buckets for all new projects. If not replaced with an alternative aggregated sink, critical logs may be lost across the entire organization, creating massive security blind spots and violating compliance requirements."
    },
    "kms_key_name": {
      "description": "The resource name for the configured Cloud KMS key.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Organization-level CMEK ensures all logs across the organization are encrypted with customer-managed keys. Required for compliance (HIPAA, SOC2, PCI DSS). Without CMEK, Google-managed encryption is used."
    },
    "organization": {
      "description": "The organization for which to retrieve or configure settings.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "This identifies which organization's logging settings are being configured. It has no direct security impact on the settings themselves."
    },
    "storage_location": {
      "description": "The storage location that Cloud Logging will use to create new resources when a location is needed but not explicitly provided.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Sets the default region Cloud Logging uses when creating new log storage resources for the folder/organization. Data residency is a sanctioned archetype: a policy should require it to be within an approved region whitelist so default log storage stays in permitted jurisdictions; the whitelist may be a hardcoded example set and is repurposable by swapping the list."
    }
  }
}
