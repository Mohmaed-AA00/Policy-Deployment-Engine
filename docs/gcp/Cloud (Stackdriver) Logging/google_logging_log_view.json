{
  "last_updated": "2026-06-23T08:26:43Z",
  "provider_version": "7.37.0",
  "arguments": {
    "bucket": {
      "description": "The bucket of the resource",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Bucket identifies which logging bucket the view belongs to. Has no direct security impact beyond resource identification."
    },
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "description": {
      "description": "Describes this view.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Description is metadata only and does not affect any security controls."
    },
    "filter": {
      "description": "Filter that restricts which log entries in a bucket are visible in this view. Filters are restricted to be a logical AND of ==/!= of any of the following: - originating project/folder/organization/billing account. - resource type - log id For example: SOURCE(\"projects/myproject\") AND resource.type = \"gce_instance\" AND LOG_ID(\"stdout\")",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Restricts which log entries in the bucket are visible through this log view (logical AND of source/resource-type/log-id matches). It narrows visibility rather than weakening it, and is a free-form expression a generic policy cannot constrain without hardcoding specific filter strings, so it is not a useful policy target."
    },
    "location": {
      "description": "The location of the resource. The supported locations are: global, us-central1, us-east1, us-west1, asia-east1, europe-west1.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Determines the region/zone where the resource is created. Policy should restrict it to an approved-region whitelist for data residency; the allowed set is parameterised (a hardcoded example whitelist is acceptable)."
    },
    "name": {
      "description": "The resource name of the view. For example: \\'projects/my-project/locations/global/buckets/my-bucket/views/my-view\\'",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "The view name is a unique identifier for the resource. It has no direct security implications beyond being a reference name."
    },
    "parent": {
      "description": "The parent of the resource.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Parent identifies the resource hierarchy location. Has no direct security impact."
    }
  }
}
