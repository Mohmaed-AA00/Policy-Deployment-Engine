{
  "last_updated": "2026-06-23T08:26:43Z",
  "provider_version": "7.37.0",
  "arguments": {
    "annotations": {
      "description": "Client-specified annotations. This is distinct from labels.\n\n**Note**: This field is non-authoritative, and will only manage the annotations present in your configuration.\nPlease refer to the field 'effective_annotations' for all of the annotations present on the resource.",
      "required": false,
      "type": "map(string)",
      "security_impact": false,
      "rationale": "Annotations provide additional metadata, but this policy set does not use annotations to enforce security controls."
    },
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "display_name": {
      "description": "Human-readable name for this resource.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Display name is mainly used as a human-readable identifier and does not directly control workstation cluster security settings."
    },
    "labels": {
      "description": "Client-specified labels that are applied to the resource and that are also propagated to the underlying Compute Engine resources.\n\n**Note**: This field is non-authoritative, and will only manage the labels present in your configuration.\nPlease refer to the field 'effective_labels' for all of the labels present on the resource.",
      "required": false,
      "type": "map(string)",
      "security_impact": false,
      "rationale": "Labels are organizational metadata for tracking ownership/cost. They do not affect access control or encryption, so they are not a security policy target."
    },
    "location": {
      "description": "The location where the workstation cluster should reside.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Determines the region/zone where the resource is created. Policy should restrict it to an approved-region whitelist for data residency; the allowed set is parameterised (a hardcoded example whitelist is acceptable)."
    },
    "network": {
      "description": "The relative resource name of the VPC network on which the instance can be accessed.\nIt is specified in the following form: \"projects/{projectNumber}/global/networks/{network_id}\".",
      "required": true,
      "type": "string",
      "security_impact": true,
      "rationale": "Workstation clusters should use approved VPC networks so that access is restricted to trusted network boundaries and managed infrastructure."
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Identifies the project the cluster is created in. This is an identifier/reference; a generic policy cannot pin which specific projects are approved without hardcoding project names, so it is not a policy target."
    },
    "subnetwork": {
      "description": "Name of the Compute Engine subnetwork in which instances associated with this cluster will be created.\nMust be part of the subnetwork specified for this cluster.",
      "required": true,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "tags": {
      "description": "Resource manager tags bound to this resource.\nFor example:\n\"123/environment\": \"production\",\n\"123/costCenter\": \"marketing\"",
      "required": false,
      "type": "map(string)",
      "security_impact": false,
      "rationale": "Tags help organise and classify resources, but this policy set does not validate resource manager tag values."
    },
    "workstation_authorization_url": {
      "description": "Specifies the redirect URL for unauthorized requests received by workstation VMs in this cluster.\nRedirects to this endpoint will send a base64 encoded 'state' query param containing the target workstation name and original request hostname. The endpoint is responsible for retrieving a token using 'GenerateAccessToken' and redirecting back to the original hostname with the token.",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "workstation_cluster_id": {
      "description": "ID to use for the workstation cluster.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Identifier for the workstation cluster. We do not constrain resource identifiers or naming conventions; doing so is not a security archetype."
    },
    "workstation_launch_url": {
      "description": "Specifies the launch URL for workstations in this cluster. Requests sent to unstarted workstations will be redirected to this URL.\nRequests redirected to the launch endpoint will be sent with a 'workstation' query parameter containing the full workstation resource. The launch endpoint is responsible for starting the workstation, polling it until it reaches 'STATE_RUNNING', and then issuing a redirect to the workstation's host URL.",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "domain_config": {
      "type": "block",
      "description": "Configuration options for a custom domain.",
      "required": false
    },
    "domain_config.domain": {
      "description": "Domain used by Workstations for HTTP ingress.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "DNS domain used for the cluster's domain config. This is a reference/naming value for the team's domain, not a security control, so it is not a policy target."
    },
    "private_cluster_config": {
      "type": "block",
      "description": "Configuration for private cluster.",
      "required": false
    },
    "private_cluster_config.allowed_projects": {
      "description": "Additional project IDs that are allowed to attach to the workstation cluster's service attachment.\nBy default, the workstation cluster's project and the VPC host project (if different) are allowed.",
      "required": false,
      "type": "list(string)",
      "security_impact": true,
      "rationale": "Lists which projects may connect to the private cluster's service attachment. A policy should require a bounded allowlist (non-empty, not overly broad) to limit who can reach the cluster; specific project IDs are not pinned."
    },
    "private_cluster_config.enable_private_endpoint": {
      "description": "Whether Workstations endpoint is private.",
      "required": true,
      "type": "bool",
      "security_impact": true,
      "rationale": "Controls whether the workstations endpoint is private. Requiring it true keeps the control plane off the public internet and within private networking, reducing exposure."
    }
  }
}
