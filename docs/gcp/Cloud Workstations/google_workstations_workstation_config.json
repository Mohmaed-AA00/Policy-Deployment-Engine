{
  "last_updated": "2026-06-23T08:26:43Z",
  "provider_version": "7.37.0",
  "arguments": {
    "annotations": {
      "description": "Client-specified annotations. This is distinct from labels.\n\n**Note**: This field is non-authoritative, and will only manage the annotations present in your configuration.\nPlease refer to the field 'effective_annotations' for all of the annotations present on the resource.",
      "required": false,
      "type": "map(string)",
      "security_impact": false,
      "rationale": "Annotations provide extra metadata about the resource, but this policy set does not use annotations to enforce security controls."
    },
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "disable_tcp_connections": {
      "description": "Disables support for plain TCP connections in the workstation. By default the service supports TCP connections via a websocket relay. Setting this option to true disables that relay, which prevents the usage of services that require plain tcp connections, such as ssh. When enabled, all communication must occur over https or wss.",
      "required": false,
      "type": "bool",
      "security_impact": true,
      "rationale": "Disables plain TCP connections (via the websocket relay) to the workstation. Forcing this off removes a network access path and reduces exposure; a policy can require it be disabled where TCP services are not needed."
    },
    "display_name": {
      "description": "Human-readable name for this resource.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Display name is mainly used as a human-readable identifier and does not directly control workstation config security settings."
    },
    "enable_audit_agent": {
      "description": "Whether to enable Linux 'auditd' logging on the workstation. When enabled, a service account must also be specified that has 'logging.buckets.write' permission on the project. Operating system audit logging is distinct from Cloud Audit Logs.",
      "required": false,
      "type": "bool",
      "security_impact": true,
      "rationale": "Enables Linux auditd OS audit logging on the workstation. A policy should require it enabled so security-relevant OS activity is logged for audit and investigation."
    },
    "idle_timeout": {
      "description": "How long to wait before automatically stopping an instance that hasn't recently received any user traffic. A value of 0 indicates that this instance should never time out from idleness. Defaults to 20 minutes.\nA duration in seconds with up to nine fractional digits, ending with 's'. Example: \"3.5s\".",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Idle timeout can affect resource usage and session duration, but this policy set does not validate idle timeout values."
    },
    "labels": {
      "description": "Client-specified labels that are applied to the resource and that are also propagated to the underlying Compute Engine resources.\n\n**Note**: This field is non-authoritative, and will only manage the labels present in your configuration.\nPlease refer to the field 'effective_labels' for all of the labels present on the resource.",
      "required": false,
      "type": "map(string)",
      "security_impact": false,
      "rationale": "Labels are mainly used for organisation, ownership, and cost tracking. This policy set does not validate workstation config label values."
    },
    "location": {
      "description": "The location where the workstation cluster config should reside.",
      "required": true,
      "type": "string",
      "security_impact": true,
      "rationale": "Determines the region/zone where the resource is created. Policy should restrict it to an approved-region whitelist for data residency; the allowed set is parameterised (a hardcoded example whitelist is acceptable)."
    },
    "max_usable_workstations": {
      "description": "Maximum number of workstations under this configuration a user can have workstations.workstation.use permission on. Only enforced on CreateWorkstation API calls on the user issuing the API request.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "Caps how many workstations can be created from the config. This is a capacity/resource-management choice, not a security control."
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "replica_zones": {
      "description": "Specifies the zones used to replicate the VM and disk resources within the region. If set, exactly two zones within the workstation cluster's region must be specified—for example, '['us-central1-a', 'us-central1-f']'.\nIf this field is empty, two default zones within the region are used. Immutable after the workstation configuration is created.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Replica zones affect availability and placement, but this policy set does not validate approved replica zones."
    },
    "running_timeout": {
      "description": "How long to wait before automatically stopping a workstation after it was started. A value of 0 indicates that workstations using this configuration should never time out from running duration. Must be greater than 0 and less than 24 hours if 'encryption_key' is set. Defaults to 12 hours.\nA duration in seconds with up to nine fractional digits, ending with 's'. Example: \"3.5s\".",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Running timeout can affect workstation lifetime and resource usage, but this policy set does not validate running timeout values."
    },
    "workstation_cluster_id": {
      "description": "The ID of the parent workstation cluster.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Identifier referencing the parent workstation cluster. This is a reference to another resource; we do not constrain which cluster is referenced, and a generic policy could not name approved clusters."
    },
    "workstation_config_id": {
      "description": "The ID to be assigned to the workstation cluster config.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Identifier for the workstation config. We do not constrain resource identifiers or naming conventions, so it is not a policy target."
    },
    "allowed_ports": {
      "type": "block",
      "description": "A list of port ranges specifying single ports or ranges of ports that are externally accessible in the workstation. Allowed ports must be one of 22, 80, or within range 1024-65535. If not specified defaults to ports 22, 80, and ports 1024-65535.",
      "required": false
    },
    "allowed_ports.first": {
      "description": "Starting port number for the current range of ports. Valid ports are 22, 80, and ports within the range 1024-65535.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "allowed_ports.last": {
      "description": "Ending port number for the current range of ports. Valid ports are 22, 80, and ports within the range 1024-65535.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "container": {
      "type": "block",
      "description": "Container that will be run for each workstation using this configuration when that workstation is started.",
      "required": false
    },
    "container.args": {
      "description": "Arguments passed to the entrypoint.",
      "required": false,
      "type": "list(string)",
      "security_impact": "true/false",
      "rationale": ""
    },
    "container.command": {
      "description": "If set, overrides the default ENTRYPOINT specified by the image.",
      "required": false,
      "type": "list(string)",
      "security_impact": "true/false",
      "rationale": ""
    },
    "container.env": {
      "description": "Environment variables passed to the container.\nThe elements are of the form \"KEY=VALUE\" for the environment variable \"KEY\" being given the value \"VALUE\".",
      "required": false,
      "type": "map(string)",
      "security_impact": "true/false",
      "rationale": ""
    },
    "container.image": {
      "description": "Docker image defining the container. This image must be accessible by the config's service account.",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "container.run_as_user": {
      "description": "If set, overrides the USER specified in the image with the given uid.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "container.working_dir": {
      "description": "If set, overrides the default DIR specified by the image.",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "encryption_key": {
      "type": "block",
      "description": "Encrypts resources of this workstation configuration using a customer-managed encryption key.\n\nIf specified, the boot disk of the Compute Engine instance and the persistent disk are encrypted using this encryption key. If this field is not set, the disks are encrypted using a generated key. Customer-managed encryption keys do not protect disk metadata.\nIf the customer-managed encryption key is rotated, when the workstation instance is stopped, the system attempts to recreate the persistent disk with the new version of the key. Be sure to keep older versions of the key until the persistent disk is recreated. Otherwise, data on the persistent disk will be lost.\nIf the encryption key is revoked, the workstation session will automatically be stopped within 7 hours.",
      "required": false
    },
    "encryption_key.kms_key": {
      "description": "The name of the Google Cloud KMS encryption key.",
      "required": true,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "encryption_key.kms_key_service_account": {
      "description": "The service account to use with the specified KMS key.",
      "required": true,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "ephemeral_directories": {
      "type": "block",
      "description": "Ephemeral directories which won't persist across workstation sessions.",
      "required": false
    },
    "ephemeral_directories.mount_path": {
      "description": "Location of this directory in the running workstation.",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "ephemeral_directories.gce_pd": {
      "type": "block",
      "description": "An EphemeralDirectory backed by a Compute Engine persistent disk.",
      "required": false
    },
    "ephemeral_directories.gce_pd.disk_type": {
      "description": "Type of the disk to use. Defaults to '\"pd-standard\"'.",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "ephemeral_directories.gce_pd.read_only": {
      "description": "Whether the disk is read only. If true, the disk may be shared by multiple VMs and 'sourceSnapshot' must be set.",
      "required": false,
      "type": "bool",
      "security_impact": "true/false",
      "rationale": ""
    },
    "ephemeral_directories.gce_pd.source_image": {
      "description": "Name of the disk image to use as the source for the disk.\n\nMust be empty 'sourceSnapshot' is set.\nUpdating 'sourceImage' will update content in the ephemeral directory after the workstation is restarted.",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "ephemeral_directories.gce_pd.source_snapshot": {
      "description": "Name of the snapshot to use as the source for the disk.\n\nMust be empty if 'sourceImage' is set.\nMust be empty if 'read_only' is false.\nUpdating 'source_snapshot' will update content in the ephemeral directory after the workstation is restarted.",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "host": {
      "type": "block",
      "description": "Runtime host for a workstation.",
      "required": false
    },
    "host.gce_instance": {
      "type": "block",
      "description": "A runtime using a Compute Engine instance.",
      "required": false
    },
    "host.gce_instance.boot_disk_size_gb": {
      "description": "Size of the boot disk in GB.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "host.gce_instance.disable_public_ip_addresses": {
      "description": "Whether instances have no public IP address.",
      "required": false,
      "type": "bool",
      "security_impact": true,
      "rationale": "Public IP addresses can expose workstation instances to external networks. Disabling public IP addresses helps keep workstation instances within approved private network access paths."
    },
    "host.gce_instance.disable_ssh": {
      "description": "Whether to disable SSH access to the VM.",
      "required": false,
      "type": "bool",
      "security_impact": true,
      "rationale": "Toggles SSH access to the workstation VM. Disabling SSH removes a direct remote-access path into the instance; a policy can require it be disabled to reduce the attack surface."
    },
    "host.gce_instance.enable_nested_virtualization": {
      "description": "Whether to enable nested virtualization on the Compute Engine VMs backing the Workstations.\n\nSee https://cloud.google.com/workstations/docs/reference/rest/v1/projects.locations.workstationClusters.workstationConfigs#GceInstance.FIELDS.enable_nested_virtualization",
      "required": false,
      "type": "bool",
      "security_impact": "true/false",
      "rationale": ""
    },
    "host.gce_instance.machine_type": {
      "description": "The name of a Compute Engine machine type.",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "host.gce_instance.pool_size": {
      "description": "Number of instances to pool for faster workstation startup.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "host.gce_instance.service_account": {
      "description": "Email address of the service account that will be used on VM instances used to support this config. This service account must have permission to pull the specified container image. If not set, VMs will run without a service account, in which case the image must be publicly accessible.",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "host.gce_instance.service_account_scopes": {
      "description": "Scopes to grant to the service_account. Various scopes are automatically added based on feature usage. When specified, users of workstations under this configuration must have 'iam.serviceAccounts.actAs' on the service account.",
      "required": false,
      "type": "list(string)",
      "security_impact": "true/false",
      "rationale": ""
    },
    "host.gce_instance.tags": {
      "description": "Network tags to add to the Compute Engine machines backing the Workstations.",
      "required": false,
      "type": "list(string)",
      "security_impact": "true/false",
      "rationale": ""
    },
    "host.gce_instance.vm_tags": {
      "description": "Resource manager tags to be bound to the VM instances backing the Workstations.\nTag keys and values have the same definition as\nhttps://docs.cloud.google.com/resource-manager/docs/tags/tags-overview\nKeys must be in the format 'tagKeys/{tag_key_id}', and\nvalues are in the format 'tagValues/456'.",
      "required": false,
      "type": "map(string)",
      "security_impact": false,
      "rationale": "Resource-manager tags applied to the workstation VM. Tags are classification/organization metadata and do not enforce security controls, so they are not a policy target."
    },
    "host.gce_instance.accelerators": {
      "type": "block",
      "description": "An accelerator card attached to the instance.",
      "required": false
    },
    "host.gce_instance.accelerators.count": {
      "description": "Number of accelerator cards exposed to the instance.",
      "required": true,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "host.gce_instance.accelerators.type": {
      "description": "Type of accelerator resource to attach to the instance, for example, \"nvidia-tesla-p100\".",
      "required": true,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "host.gce_instance.boost_configs": {
      "type": "block",
      "description": "A list of the boost configurations that workstations created using this workstation configuration are allowed to use.",
      "required": false
    },
    "host.gce_instance.boost_configs.boot_disk_size_gb": {
      "description": "Size of the boot disk in GB. The minimum boot disk size is '30' GB. Defaults to '50' GB.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "host.gce_instance.boost_configs.enable_nested_virtualization": {
      "description": "Whether to enable nested virtualization on the Compute Engine VMs backing boosted Workstations.\n\nSee https://cloud.google.com/workstations/docs/reference/rest/v1/projects.locations.workstationClusters.workstationConfigs#GceInstance.FIELDS.enable_nested_virtualization",
      "required": false,
      "type": "bool",
      "security_impact": "true/false",
      "rationale": ""
    },
    "host.gce_instance.boost_configs.id": {
      "description": "The id to be used for the boost config.",
      "required": true,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "host.gce_instance.boost_configs.machine_type": {
      "description": "The type of machine that boosted VM instances will use—for example, e2-standard-4. For more information about machine types that Cloud Workstations supports, see the list of available machine types https://cloud.google.com/workstations/docs/available-machine-types. Defaults to e2-standard-4.",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "host.gce_instance.boost_configs.pool_size": {
      "description": "Number of instances to pool for faster workstation boosting.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "host.gce_instance.boost_configs.accelerators": {
      "type": "block",
      "description": "An accelerator card attached to the boost instance.",
      "required": false
    },
    "host.gce_instance.boost_configs.accelerators.count": {
      "description": "Number of accelerator cards exposed to the instance.",
      "required": true,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "host.gce_instance.boost_configs.accelerators.type": {
      "description": "Type of accelerator resource to attach to the instance, for example, \"nvidia-tesla-p100\".",
      "required": true,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "host.gce_instance.confidential_instance_config": {
      "type": "block",
      "description": "A set of Compute Engine Confidential VM instance options.",
      "required": false
    },
    "host.gce_instance.confidential_instance_config.enable_confidential_compute": {
      "description": "Whether the instance has confidential compute enabled.",
      "required": false,
      "type": "bool",
      "security_impact": "true/false",
      "rationale": ""
    },
    "host.gce_instance.shielded_instance_config": {
      "type": "block",
      "description": "A set of Compute Engine Shielded instance options.",
      "required": false
    },
    "host.gce_instance.shielded_instance_config.enable_integrity_monitoring": {
      "description": "Whether the instance has integrity monitoring enabled.",
      "required": false,
      "type": "bool",
      "security_impact": "true/false",
      "rationale": ""
    },
    "host.gce_instance.shielded_instance_config.enable_secure_boot": {
      "description": "Whether the instance has Secure Boot enabled.",
      "required": false,
      "type": "bool",
      "security_impact": "true/false",
      "rationale": ""
    },
    "host.gce_instance.shielded_instance_config.enable_vtpm": {
      "description": "Whether the instance has the vTPM enabled.",
      "required": false,
      "type": "bool",
      "security_impact": "true/false",
      "rationale": ""
    },
    "persistent_directories": {
      "type": "block",
      "description": "Directories to persist across workstation sessions.",
      "required": false
    },
    "persistent_directories.mount_path": {
      "description": "Location of this directory in the running workstation.",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "persistent_directories.gce_hd": {
      "type": "block",
      "description": "A directory to persist across workstation sessions, backed by a Compute Engine Hyperdisk Balanced High Availability disk.",
      "required": false
    },
    "persistent_directories.gce_hd.archive_timeout": {
      "description": "How long to wait before converting the disk into a snapshot.",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "persistent_directories.gce_hd.reclaim_policy": {
      "description": "Whether the persistent disk should be deleted when the workstation is deleted. Possible values: [\"DELETE\", \"RETAIN\"]",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "persistent_directories.gce_hd.size_gb": {
      "description": "The GB capacity of a persistent home directory. Defaults to '200'.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "persistent_directories.gce_hd.source_snapshot": {
      "description": "Name of the snapshot to use as the source for the disk.",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "persistent_directories.gce_pd": {
      "type": "block",
      "description": "A directory to persist across workstation sessions, backed by a Compute Engine regional persistent disk. Can only be updated if not empty during creation.",
      "required": false
    },
    "persistent_directories.gce_pd.disk_type": {
      "description": "The type of the persistent disk for the home directory. Defaults to 'pd-standard'.",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "persistent_directories.gce_pd.fs_type": {
      "description": "Type of file system that the disk should be formatted with. The workstation image must support this file system type. Must be empty if 'sourceSnapshot' is set. Defaults to 'ext4'.",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "persistent_directories.gce_pd.reclaim_policy": {
      "description": "Whether the persistent disk should be deleted when the workstation is deleted. Valid values are 'DELETE' and 'RETAIN'. Defaults to 'DELETE'. Possible values: [\"DELETE\", \"RETAIN\"]",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "persistent_directories.gce_pd.size_gb": {
      "description": "The GB capacity of a persistent home directory for each workstation created with this configuration. Must be empty if 'sourceSnapshot' is set.\nValid values are '10', '50', '100', '200', '500', or '1000'. Defaults to '200'. If less than '200' GB, the 'diskType' must be 'pd-balanced' or 'pd-ssd'.",
      "required": false,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    },
    "persistent_directories.gce_pd.source_snapshot": {
      "description": "Name of the snapshot to use as the source for the disk. This can be the snapshot's 'self_link', 'id', or a string in the format of 'projects/{project}/global/snapshots/{snapshot}'. If set, 'sizeGb' and 'fsType' must be empty. Can only be updated if it has an existing value.",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "readiness_checks": {
      "type": "block",
      "description": "Readiness checks to be performed on a workstation.",
      "required": false
    },
    "readiness_checks.path": {
      "description": "Path to which the request should be sent.",
      "required": true,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "readiness_checks.port": {
      "description": "Port to which the request should be sent.",
      "required": true,
      "type": "number",
      "security_impact": "true/false",
      "rationale": ""
    }
  }
}
