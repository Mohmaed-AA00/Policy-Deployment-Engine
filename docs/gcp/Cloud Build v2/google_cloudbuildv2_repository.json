{
  "last_updated": "2026-06-23T08:26:43Z",
  "provider_version": "7.37.0",
  "arguments": {
    "annotations": {
      "description": "Allows clients to store small amounts of arbitrary data.\n\n**Note**: This field is non-authoritative, and will only manage the annotations present in your configuration.\nPlease refer to the field 'effective_annotations' for all of the annotations present on the resource.",
      "required": false,
      "type": "map(string)",
      "security_impact": false,
      "rationale": "Stores small amounts of arbitrary client metadata (key/value). Like labels, annotations are descriptive metadata; a generic security policy does not constrain their contents."
    },
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": "true/false",
      "rationale": ""
    },
    "location": {
      "description": "The location for the resource",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Determines the region/zone where the resource is created. Policy should restrict it to an approved-region whitelist for data residency; the allowed set is parameterised (a hardcoded example whitelist is acceptable)."
    },
    "name": {
      "description": "Name of the repository.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "The repository name itself does not directly impact security. However, enforcing naming conventions can help with resource identification, organisation, and management."
    },
    "parent_connection": {
      "description": "The connection for the resource",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "References the parent Cloud Build connection the repository belongs to. This is a reference/identifier to another resource; we do not constrain what a resource points at."
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Identifier of the GCP project the repository belongs to. This is a project reference; we do not constrain which project a resource is created in."
    },
    "remote_uri": {
      "description": "Required. Git Clone HTTPS URI.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "The Git HTTPS clone URI of the external repository being linked. This is a reference to another resource; we do not constrain which repository it points at, and a generic policy cannot pin team-specific repo URLs."
    }
  }
}
